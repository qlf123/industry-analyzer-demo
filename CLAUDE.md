# 项目说明
- 页面主文件：panorama.html；构建脚本：build_panorama.py；模板：panorama_template.html
- 产品需求文档：PRD-行业分析模块.md（唯一有效版本）

# 工作规则
- 每次修改页面后，把改动同步到 PRD-行业分析模块.md 对应章节，并列出改了哪几条给我确认后再写入。
- PRD 只按产品视角写：页面做成什么样、交互结果、数据口径、显示/不显示什么、验收标准；不写函数名、数据结构、实现方式。
- 遇到需要技术决策的地方，列出现状、影响和可选项，由我决定，不要直接改。
- 「行业全景」页签不改动，除非我明确要求。
- 页面上不出现「示例数据」「待确认」类标注。
