# 双图谱映射审核台 PRD

版本：V1.0

产品模块：双图谱映射

文档性质：页面功能说明

## 1. 使用角色

| 角色 | 页面使用范围 |
| --- | --- |
| 图谱管理员 | 查看映射任务、筛选候选、批量处理 |
| 专业教师 | 审核教学内容与能力项的支撑关系 |
| 行业专家 | 审核岗位能力、复杂情境、法规安全等关系 |
| 平台管理员 | 管理审核权限和审核记录 |

## 2. 页面入口

审核台作为现有图谱平台的一级菜单使用：

图谱平台 → 图谱映射审核台

当前菜单需要高亮，并显示待审核数量。

## 3. 页面整体结构

页面采用平台统一的紧凑型后台样式，依次包含：

1. 平台导航（按照现在的结构，先放顶部即可，以UI为准）
2. 顶部上下文与页面工具栏
3. 审核状态标签
4. 搜索、筛选与批量操作区
5. 映射候选列表
6. 分页区

本页面只负责候选映射审核，不编辑教学知识体系和行业能力图谱原始内容。

## 4. 审核状态

| 状态 | 说明 |
| --- | --- |
| 待审核 | 已生成候选关系，尚未完成审核；包括需要专家重点判断和复审中的关系 |
| 已确认 | 专家确认，形成正式映射关系 |
| 已驳回 | 专家认为不构成有效映射 |

状态标签显示各状态数量。点击状态标签后，列表只展示对应状态的数据。

### 4.1 状态区分依据

映射状态表示审核流程所处阶段，不表示映射类型、服务等级、覆盖度或学生能力达成结果。系统根据以下条件区分状态：

| 区分维度 | 判断内容 |
| --- | --- |
| 是否生成成功 | 原始节点、能力项、关系类型、等级、置信度和判定依据是否完整 |
| 是否通过规则校验 | ID 是否存在、字段格式是否正确、关系类型和服务等级是否符合规则 |
| 是否需要专家介入 | 置信度、证据完整性、特殊领域和关系边界是否触发专家审核 |
| 是否已有审核结论 | 专家是否确认、驳回或退回修改 |

状态与字段的关系：

- 待审核包含普通待审、需要专家标注和复审中的候选状态。
- 已确认才表示该映射进入正式关系范围。
- 已驳回表示该候选不计入正式能力覆盖率。
- 专家标注和复审中属于待审核状态的内部标记，不单独展示为顶部状态。
- 映射类型、服务等级和覆盖度是候选或审核字段，修改字段本身不会自动等同于审核确认。

### 4.2 状态流转规则

#### 初始进入

- LLM 生成结果字段缺失、无法关联原始节点或规则校验失败：不进入审核状态列表，标记为生成失败并重新处理。
- 规则校验通过，且证据完整、关系边界清晰、未触发专家条件：进入“待审核”。
- 规则校验通过，但置信度低于 0.75、证据不足、涉及法规/安全/合同/投诉时限、关系边界不清晰或其他特殊规则命中：进入“待审核”，并记录“需要专家标注”的内部标记。

#### 审核中流转

| 当前状态 | 操作或条件 | 目标状态 | 约束 |
| --- | --- | --- | --- |
| 待审核 | 专家确认 | 已确认 | 记录审核人、审核时间和最终字段 |
| 待审核 | 专家驳回 | 已驳回 | 必须填写驳回原因 |
| 待审核 | 退回补充或修改 | 待审核 | 保留复审中的内部标记和修改意见 |

#### 终态规则

- 已确认和已驳回是本轮审核的终态，普通页面操作不能直接回退。
- 已确认关系如需重新处理，应通过重新生成或管理员重开流程产生新的审核记录，不覆盖原审核留痕。
- 已驳回关系不得进入正式能力覆盖率，也不得被批量确认。
- 批量确认只允许处理当前页中满足批量确认条件的“待审核”候选；已确认和已驳回状态不得批量确认。

## 5. 搜索、筛选与批量操作

### 5.1 搜索

支持搜索：

- 教学节点名称
- 教学节点 ID
- 能力项名称
- 能力项 ID
- 所属课程

输入关键词后，列表实时或提交后刷新结果，并回到第 1 页。

### 5.2 筛选

支持按映射类型筛选：

- 全部映射类型
- 直接支撑
- 部分支撑
- 基础支撑
- 提供达标判据
- 情境案例
- 相关但不构成覆盖

后续可增加课程、教学节点类型、能力领域和服务等级筛选。

### 5.3 批量操作

- 支持勾选当前页全部候选。
- 支持取消当前页全选。
- 支持批量确认已勾选候选。
- 批量确认前明确提示确认数量。
- 已驳回或已确认的数据不可重复批量确认。
- 未选择候选时提示“请先选择候选映射”。

## 6. 映射候选列表

每一行代表一条“教学节点 → 行业能力项”的候选映射。

### 6.1 列表字段

| 字段 | 展示内容 | 是否可编辑 |
| --- | --- | --- |
| 选择框 | 选择当前候选 | 是 |
| 知识点 | 教学节点名称、节点 ID、节点类型 | 否 |
| 能力项 | 能力名称、能力 ID、所属课程或能力领域 | 否 |
| 映射类型 | 当前候选的关系类型 | 是 |
| 服务等级 | L1、L2、L3 | 是 |
| 置信度 | 系统对判断结果的可信程度 | 否 |
| AI 判定依据 | 原文证据和语义判断说明 | 否 |
| 来源 / 审核人 | 候选来源、算法标记、审核人信息 | 否 |
| 操作 | 确认、驳回、退回修改 | 是 |

### 6.2 教学节点展示

教学节点至少展示：

- 节点名称
- 节点 ID
- 节点类型
- 所属专业、课程、任务路径

### 6.3 能力项展示

能力项至少展示：

- 能力名称
- 能力 ID
- 能力类型
- 能力领域
- 适用岗位或岗位上下文

## 7. 审核操作

### 7.1 确认

点击“确认”后：

- 当前映射状态变为“已确认”。
- 记录审核人和审核时间。
- 映射关系进入正式关系查询范围。

### 7.2 调整映射字段

专家可以调整：

- 映射类型
- 服务等级
- 覆盖度

调整后保留 AI 原始值、专家最终值和调整意见。

### 7.3 驳回

点击“驳回”后展开驳回区域，必须填写驳回原因。

建议原因：

- 语义相关但不构成教学支撑关系。
- 映射目标能力不准确。
- 证据不足，需要补充原始资料。
- 能力等级判断不准确。

驳回后状态变为“已驳回”，该关系不计入正式能力覆盖率。

### 7.4 退回修改

退回修改时必须填写修改意见，状态变为“复审中”。

## 8. 分页规则

- 默认每页显示 10 条。
- 支持切换每页 10、20、50 条。
- 支持上一页、下一页和页码跳转。
- 搜索、筛选、切换审核状态后回到第 1 页。
- “全选”只作用于当前页。
- 列表底部展示候选总数和当前页码。

## 9. 异常与空状态

| 场景 | 页面表现 |
| --- | --- |
| 当前状态无数据 | 显示“当前状态下没有匹配的候选映射” |
| 搜索无结果 | 显示空结果提示，并保留筛选条件 |
| LLM 结果字段缺失 | 不进入审核列表，标记为生成失败 |
| 找不到原始节点 | 不允许确认，提示补充或重新生成 |
| 批量确认未选择数据 | 提示“请先选择候选映射” |
| 驳回未填写原因 | 不允许提交驳回 |

## 10. 数据留痕

页面审核操作需要保留：

- 教学节点原始 ID
- 能力项原始 ID
- AI 生成的关系类型、服务等级、覆盖度、置信度
- AI 判定依据
- 专家最终关系类型、服务等级和审核意见
- 审核人、审核时间和审核状态
- 驳回或退回原因

## 11. 页面验收标准

- 审核台可以从平台侧边菜单进入。
- 当前菜单有明确高亮状态。
- 页面展示状态数量、筛选区、候选列表和分页区。
- 列表字段与页面字段定义保持一致。
- 可以确认单条映射。
- 可以修改关系类型、服务等级和覆盖度。
- 可以驳回映射，且驳回必须填写原因。
- 可以退回修改，且必须填写修改意见。
- 可以勾选当前页全部候选。
- 可以批量确认已选候选。
- 审核状态和审核意见可以留痕。
