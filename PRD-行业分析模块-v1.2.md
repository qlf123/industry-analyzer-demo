# PRD · 行业分析模块

**产品**：AIGC 智慧课堂教学平台 · 管理端
**模块**：功能模块 1「行业分析」
**版本**：**v1.2**（对照实际构建核实）
**状态**：可点高保真原型已完成，待评审；含 2 个待修 bug
**日期**：2026-09-14

---

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-09 | 首版。能力测评为「三选一数据来源 + 三步式」 |
| v1.1 | 2026-09-10 | 按实施提示词规格撰写四层渐进式（未核实） |
| **v1.2** | **2026-09-14** | **对照实际构建逐项核实并修正；补入能力提升路径卡（已实现）；新增第十一章「已发现的问题」，含 2 个待修 bug 与 6 项优化建议** |

---

## 一、背景与定位

### 1.1 为什么要有这个模块

职业院校每年都要回答四个问题，而这四个问题目前都缺少可核对的产业侧依据：

| 学校要做的事 | 现状痛点 |
|---|---|
| 专业设置动态调整（报什么、撤什么） | 依据来自经验与零散调研，写不出可追溯的论证 |
| 人才培养方案修订（加哪门课、砍哪门课） | 不知道产业实际要什么能力、要到什么深度 |
| 实训条件建设经费投向 | 缺少"为什么要买这台设备"的能力级依据 |
| 双高 / 提质培优 / 产教融合共同体申报 | "对接产业需求"一章无实证材料 |

这四件事有个共同点：**结论都要拿给别人看**。所以本模块的产出不只是"能在线浏览"，
而必须**能带走、能被核对**。

### 1.2 与「图谱平台」的分工

| | 图谱平台（另一独立平台） | 本模块（行业分析） |
|---|---|---|
| 干什么 | **造图并确认** | **用图** |
| 谁在用 | 业务方 / 行业专家 | 校领导 / 专业负责人 / 教务 |
| 是否需要拍板 | 需要，且在那边一次性定好 | **不需要** |

> **核心原则**：本模块只呈现已确认的数据，不出现让用户拍板的内容。
> **唯一例外**是用户描述自身情况的输入（选专业、选岗位、勾课程、勾资源）——那是他自己的事实。

### 1.3 在整体产品中的位置

```
AIGC 智慧课堂教学平台
├─ 教师端工作台        备课、生成课件教案（另有 PRD）
└─ 管理端
    ├─ 数据概览
    ├─ 行业分析        ← 本 PRD
    ├─ 教务基础数据    （实训室清单在此维护）
    ├─ 课程计划管理    （学时、开课学期在此决策）
    ├─ 账号与角色
    └─ 系统配置
```

---

## 二、用户与场景

### 2.1 目标用户

| 角色 | 关心什么 | 主要动作 |
|---|---|---|
| **校领导 / 分管教学副校长** | 专业布局对不对得上产业 | 看行业全景 → 看概况总览 → 导出这一页 |
| **专业负责人 / 系主任** | 缺什么能力、课怎么改 | 走四层测评 → 看处方（按能力视图） |
| **教务处** | 补课占多少课时、排哪学期 | 看处方（按课程视图）→ 转入课程计划管理 |
| **企业方 / 评估专家**（只读） | 这份数据可不可信 | 看数据可信度、点开来源证据 |

### 2.2 核心场景

**S1 · 了解一个行业**（零输入）— 输入行业名，看到完整产业结构与新兴能力方向
**S2 · 一次专业体检**（渐进输入）— 四层逐步深入，每层结束都有可用报告，随时可停
**S3 · 拿走用** — 概况总览可单独导出；完整报告可导出 Markdown
**S4 · 追溯核对** — 任一结论可点开看能力项与来源证据

### 2.3 非目标

学生与家长；**同类院校横向对比**（无数据，明确不做）

### 2.4 「差距」的三种基准

| 基准 | 本期是否提供 |
|---|---|
| **产业岗位要求**（能力图谱） | ✅ |
| **国家专业教学标准**（应教什么） | ✅ v1.1 起 |
| 同类院校水平 | ❌ 无数据 |

> 两者叠加才有杀伤力：「这几条国家标准里没写，但多个岗位在招聘时都要」——
> 既是新信息又有依据，正是申报材料需要的素材。

---

## 三、功能范围

### 3.1 本期做（均已实现）

- [x] 行业检索与切换
- [x] 四层能力全景（环形 / 树形 / 卡片）
- [x] 能力矩阵
- [x] 产业趋势与新兴能力
- [x] 数据口径与可信度说明
- [x] **能力测评 · 四层渐进式**
- [x] **概况总览（三个分层数 + 规则化人话结论）**
- [x] **能力要求清单 + 能力雷达图（可按岗位切换）**
- [x] **专业教学标准符合度**
- [x] **缺口处置建议 · 五档排序 · 按课程 / 按能力双视图**
- [x] **能力提升路径卡（观测点差集 + 六块内容）**
- [x] 诊断结果叠加回行业全景
- [x] 版本历史、Markdown 导出、打印视图
- [x] 图谱平台构建流水线演示（收在二级入口）

### 3.2 已废弃

| 废弃项 | 原因 | 当前状态 |
|---|---|---|
| **「本校能力达成填报」（V1 三步式）** | 要求逐条填 28 项"教到 L1/L2/L3"。**教师自己说不准**，填出来是噪音 | tab 标记为 `能力测评0.1` 且 `hide`，**代码仍在**，见 §11 优化项 O-1 |

### 3.3 本期不做

同类院校对比；学时 / 开课学期 / 课程名的自动生成；人才培养方案真实解析（仅说明入口）；
行业图谱的构建与编辑；多专业群组群合理性评估

---

## 四、信息架构

```
行业分析
├─ 顶部：行业输入框（通栏）+「分析」按钮
├─ 行业全景
│   ├─ 能力图谱   环形 / 树形（默认）/ 卡片
│   ├─ 能力矩阵   岗位 × 能力项
│   └─ 趋势与新兴 产业趋势 + 新兴能力项
├─ 能力测评（四层渐进式）
│   ├─ 常驻头条   专业 / 时间 / 第 N 层 / 面向岗位数 / 保留课数 ｜ 叠加 · 历史 · 导出
│   ├─ 第 1 层  选专业
│   ├─ 第 2 层  选择目标岗位
│   ├─ 第 3 层  选择已设课程
│   ├─ 第 4 层  选择已有资源
│   └─ 诊断报告
│       ├─ 概况总览          三个分层数 + 人话结论 +「导出这一页」
│       ├─ 能力要求清单       按核心 / 被要求岗位数排序
│       ├─ 能力雷达图         可按岗位切换
│       ├─ 专业教学标准符合度
│       ├─ 诊断结论
│       └─ 缺口处置建议       五档 ｜ 按课程 / 按能力
└─ 页脚说明标签
```

---

## 五、功能详述

### 5.1~5.4 行业检索 / 能力全景 / 能力矩阵 / 趋势与新兴

（与 v1.0 一致，实现无变化，略。要点保留如下）

- 未建成的行业不出现在下拉，避免点进死胡同；输入后点「分析」给「尚未建图」抽屉
- 能力图谱三形态切换器在卡片右上角，切换时右侧统计栏常驻
- 未纳入本期范围的子行业以斜纹空楔形表示，**不显示为"待拍板"**
- **产业趋势必须标注无来源证据**，视觉上以虚线框、次要权重呈现
- 能力矩阵 tab 计数显示为维度 `9 × 28`

---

### 5.5 能力测评 · 四层渐进式

#### 5.5.0 设计范式

**系统先按国家专业教学标准摆出"标准答案"，用户只做减法与修正。**

> 「按《导游服务》专业教学标准，这个专业面向这 3 个岗位、开这 4 门课。哪些你们没有？」

**已核实**：选定专业后，第 3 层的标准课程表默认全部预勾（头条即显示「保留 4 门课」），
用户动作是取消，符合"做减法"范式。

#### 5.5.1 分层结构（已核实）

| 层 | 名称 | 解锁 |
|---|---|---|
| 第 1 层 | **选专业** | 岗位面向（第一个数） |
| 第 2 层 | **选择目标岗位** | 能力要求清单与优先级 |
| 第 3 层 | **选择已设课程** | 内容完整 / 培养深度（第二、三个数）+ 雷达 + 符合度 + 结论 + 处方 |
| 第 4 层 | **选择已有资源** | 处方中的实训条件比对 |

**交互规则（已核实）**

- 步骤卡片四态：`active`（进行中）/ `done`（已完成，收起为一行摘要）/ `locked`（待办）
- 已完成卡摘要显示本层结果与**「已解锁：×××」**
- 常驻头条随层级实时更新：`导游服务 ｜ 时间 ｜ 第 3 / 4 层 · 面向 3 个岗位 · 保留 4 门课`
- **每层结束都有完整可用报告**：第 1 层 350 字符（仅概况总览）→ 第 2 层 932 → 第 3 层 4179 → 第 4 层 4361

#### 5.5.2 第 1 层 · 选专业

卡片式陈列，每张显示专业名、层次 · 代码、培养目标。支持多选（多个相关专业）。
旁有次要入口「上传人才培养方案（快速通道）」。

**标准对接岗位必须显示原文表述与匹配方式**——报告的符合度一节中呈现为
`标准原文「地陪导游」· exact 精确匹配`，让用户可核对 Gate 1 的结果。

#### 5.5.3 第 2 层 · 选择目标岗位

标准对接岗位预选，用户可增可减。

#### 5.5.4 第 3 层 · 选择已设课程

标准课程表预勾，用户取消没开的；可展开勾到知识点 / 技能规范一级。
**不显示学时与开课学期**（平台不掌握某校排课）。
**不询问"教到什么程度"**——达成等级由课程内知识节点沿映射边推出。

#### 5.5.5 第 4 层 · 选择已有资源（选填）

从平台实训条件字典勾选本校已有的。

> ⚠️ **当前实现有两处问题，见 §11 的 B-1、O-2。**

#### 5.5.6 快速通道

**已核实**：仅为说明入口，未做真解析，符合设计意图（不做假的解析动画）。

---

### 5.6 诊断报告

#### 5.6.1 概况总览

**不给单一"体检分"**——分数无可比基准，且会掩盖结构差异。

**三个分层的数**（已核实）：

| 层 | 实测显示 | 对应决策 |
|---|---|---|
| 岗位面向 | `3 / 9` · 市场 9 个岗位，你面向 3 个 | 要不要拓专业方向（要报备，最贵） |
| 内容完整 | `17 / 18` · 面向的岗位要 18 条能力，你覆盖 17 条 | 要不要加课（改方案） |
| 培养深度 | `3 / 17` · 覆盖的 17 条里，达到岗位要求高度的 3 条 | 要不要加实训（花钱） |

**人话结论**由规则生成，阈值为常量 `V2C`（已核实）：

```
COVERAGE_OK  0.70   覆盖率 ≥70% → 视为「教到了」
ATTAIN_OK    0.50   达标率 ≥50% → 视为深度足够
COVERAGE_LOW 0.50
POS_NARROW   0.40   岗位面向 <40% → 面向偏窄
CORE_GAP_MIN 3      核心能力缺口 ≥3 条
POS_COV_LOW  0.40   单岗位覆盖率 <40% → 处方第五档
```

实测输出：

> 导游服务专业，面向 3 个目标岗位（地接导游、景区讲解员、全陪领队）。
> **课程覆盖较全（17/18），但达标深度不足——问题不在「没教」，在「教得浅」**；
> 核心优势集中在「讲解与表达、服务与沟通」方向。

**落脚点为结构导向**（先描述结构，再指出差距），符合 v1.1 待定项 B 的默认值。

**「导出这一页」**按钮已实现。

#### 5.6.2 能力要求清单

按核心 / 被要求岗位数排序，每条显示：名称、★核心、类型、`现状 → 目标`、被几个岗位要求。

#### 5.6.3 能力雷达图

- 轴 = 单条能力项，灰虚线 = 目标岗位要求最高等级，紫块 = 本校达成，红点 = 缺口轴
- 可按「全部 / 各目标岗位」切换
- 轴按核心优先、要求等级从高到低取前 12 条，超出部分提示"另有 N 条未画"

#### 5.6.4 专业教学标准符合度

两块：

- **标准课程 · 开齐了吗** —— 逐条 ✓ 已开 / △ 标准要求但课程体系暂无
  （后者注明"暂无法比对，需向图谱平台反馈补建该课程"）
- **标准对接岗位 · 课程支撑得住吗** —— 逐岗位支撑度百分比 + 标准原文 + 匹配方式

> ⚠️ **支撑度的口径未在界面写明，与"培养深度"存在观感冲突，见 §11 的 O-3。**

#### 5.6.5 诊断结论

按内容缺口 / 深度缺口分类，说明最需要补的是哪几条、是否含核心能力。

#### 5.6.6 缺口处置建议 · 五档 · 双视图

**五档排序即回答"先做哪一件"**（已核实档位 1、3、4 触发）：

| 序 | 档位 | 判定 | 成本 |
|---|---|---|---|
| 1 | 现有课加深 | 已开课程覆盖但没教到岗位要求高度 | 改教学设计 · 零成本 |
| 2 | 现有课扩内容 | 已开课程内有未勾选的知识节点能补 | 改大纲 · 低成本 |
| 3 | 新增课程模块 | 课程体系里有课能覆盖，但本校没开 | 要排课时 · 中成本 |
| 4 | 新建实训条件 | 要开的课有实训要求而本校无对应实训室 | 要花钱 · 高成本 |
| 5 | 新增专业方向 | 单岗位覆盖率 < `POS_COV_LOW` | 要报备 · 最贵 |

**双视图切换**：

- **按课程**（默认，教务视角）—— 每条给目标等级行为描述、承接课、达标考核、依据文件
- **按能力**（专业带头人视角）—— 能力提升路径卡，见下

**不生成**：学时、开课学期、课程名、设备型号。

#### 5.6.7 能力提升路径卡（按能力视图）

**核心思路**：「怎么提升」= 三级行为化描述中，现状等级与目标等级之间的**观测点差集**。

每张卡六块（已核实，实测输出完整）：

| 块 | 内容 | 出处 |
|---|---|---|
| **① 差在哪** | 「现在能做到 · L1」vs「**还要能做到 · L2（要补的教学目标）**」 | `levels[].points`，标注"原样取自，未改写" |
| **② 在哪教** | 课程 › 模块 › 知识点 + 服务等级 + 节点数 | 课程映射边 |
| **③ 怎么教** | 考核方式原文 + 教学形式提示 + 「这条建议按哪条规则推出」可展开 | `ASSESS_RULES` 规则表，**标注"非图谱原生数据"** |
| **④ 需要什么条件** | 实训条件比对，或「承接课程无实训条件要求」 | 课程 `practiceReq` |
| **⑤ 先补什么** | 前置能力，标出「前置也在缺口」并给出依据 | `prerequisites` |
| **⑥ 为什么值得补** | 被几个岗位要求、几个要求最高等级、是否核心、出现于哪些子行业 | 派生统计，**不替用户打分** |

**已核实的关键细节**：规则未命中时输出
「该考核方式未命中已有规则，教学形式由任课教师判断。」——**不硬凑**，符合设计要求。

---

### 5.7 叠加、版本与导出

叠加绑定到产出它的那一版报告；版本历史记录历次诊断；支持 Markdown 导出与打印视图。

---

## 六、数据依赖与口径

### 6.1 数据来源

| 数据 | 来源 | 文件 |
|---|---|---|
| 行业能力图谱（四层） | 图谱平台产出并确认 | `data_ability.py` `panorama_ext.py` |
| **专业教学标准** | 国家专业教学标准，结构化维护 | `data_major_standard.py` |
| 平台课程体系 | 按专业教学标准建 | `data_courselib.py` |
| 本校知识图谱（示例） | 学校自建并接入 | `data_knowledge.py` |
| 实训条件字典 | 平台维护 | `data_courselib.py` |

### 6.2 专业教学标准数据结构（已核实实现）

```python
{
  "code": "740102", "name": "导游服务", "stage": "中职", "years": 3,
  "source": "中等职业学校导游服务专业教学标准（2022 年修订）",
  "goal":   "培养面向……的高素质技术技能人才。",
  "positions": [                                    # Gate 1 的显式结果
    {"id": "POS-01", "stdText": "地陪导游",   "match": "exact"},
    {"id": "POS-03", "stdText": "景区讲解员", "match": "近义"},
  ],
  "courses": ["CRS-01", "CRS-02", "CRS-03", "CRS-07"],
  "coursesUnmapped": ["旅游政策与法规"],            # 如实列出，不硬凑
}
```

三条硬性要求**均已落实**：`stdText`+`match` 必填、`coursesUnmapped` 如实列出、Gate 1 为显式数据。

### 6.3 教学形式规则表（已核实实现）

```python
ASSESS_RULES = [
  {"keys": ["沙盘","演练","模拟","情境"], "hint": "需要情境模拟环节",      "why": "……"},
  {"keys": ["评审","报告","方案","作品","包"], "hint": "需要有产出物",     "why": "……"},
  {"keys": ["答辩","宣讲","授课"],       "hint": "需要表达与被质询的环节", "why": "……"},
  {"keys": ["实操","录入","操作","记录"], "hint": "需要真实系统或等效实训环境", "why": "……"},
  {"keys": ["复盘","迭代","优化","指导他人"], "hint": "需要多轮次与带教环节", "why": "……"},
]
```

写在数据文件、界面可展开看理由、显式标注非原生数据——三项要求均已满足。

### 6.4 关键口径

| 口径 | 定义 | 状态 |
|---|---|---|
| **核心能力** | 被 2 个以上岗位要求 **且** 至少一条国家级来源 | 按 V2 方案实现；口径待确认，见 §10 |
| **能力等级** | L1 认知/辅助级、L2 独立操作级、L3 综合应用级 | 方案已定义 |
| **达成等级** | 已勾选知识节点沿 covers / partially_covers 边推出的最高服务等级 | 本模块定义 |
| **达标** | 达成等级 ≥ 岗位要求等级 | 本模块定义 |
| **岗位面向** | 目标岗位数 / 行业岗位总数 | v1.1 新增 |
| **内容完整** | 有任一支撑的能力项数 / 目标岗位要求的去重能力项数 | v1.1 新增 |
| **培养深度** | 达标能力项数 / 有支撑的能力项数 | v1.1 新增 |
| **岗位支撑度** | 该岗位要求的能力中有课程覆盖的比例（**不含深度**） | ⚠️ 界面未写明，见 §11 O-3 |

### 6.5 实测数据规模

```
行业侧（旅游业 v1.3）
  子行业 4（已建图 3）· 岗位 9 · 能力项 28 · 岗位-能力关系 103
  领域 8 · 核心能力 12 · 跨子行业能力 24
  来源证据 50 条（国家级 13 / 行业标准 16 / 招聘 21）· 无文献直接依据 7.1%

教育侧
  课程体系 7 门课 / 102 个知识节点 / 118 条映射边 / 可支撑 27 条能力项
  专业教学标准 3 个专业 · 实训条件字典 10 项 · 教学形式规则 5 条
  本校知识图谱（示例）91 条映射边

构建产物
  panorama.html 474 KB（其中数据 116 KB）· 模板 4850 行
```

---

## 七、设计原则

**评审时应作为验收标准。**

1. **只呈现已确认的数据。** 需要拍板的事在图谱平台完成。唯一例外是用户描述自身情况的输入。
2. **不生成没有依据的内容。** 学时、开课学期、课程名、设备型号一律不编。
3. **不问用户答不上来的问题。** 不问"这门课教到什么程度"，只问"有没有"。
4. **让用户做减法而不是列举。** 先摆标准答案，用户只修正差异。
5. **如实标注精度边界。** 说明当前只能给到课程级，接入后可精确到知识点级。
6. **不做同类院校对比。** 无数据，不得暗示具备该能力。
7. **不把覆盖率当好坏判断。** 先描述结构，再让用户判断。
8. **不静默改变用户已看到的结论。** 设置变更时提示而非清空。
9. **未填报 ≠ 没有。**〔v1.2 新增，见 §11 B-1〕 用户跳过的选填项不得被判定为"缺"。
10. **同一份报告里的两个数不得口径打架。**〔v1.2 新增，见 §11 O-3〕 并列出现的指标必须各自写明口径。

---

## 八、非功能要求

| 项 | 要求 | 实测 |
|---|---|---|
| 形态 | 单页应用，嵌入既有管理端左侧菜单 | ✅ |
| 依赖 | 单文件零依赖 | ✅ |
| 响应式 | ≥1000px 为主，支持窄屏降级 | ✅ |
| 主题 | 亮 / 暗色 | ✅ |
| 可访问性 | 键盘可操作、ARIA 状态、焦点可见 | ✅ |
| 打印 | 概况总览可单独导出 | ✅ |
| 运行时错误 | 四层全流程 `pageerror` 为空 | ✅ |
| 性能 | 数据随页面下发 116 KB | 正式版应改为按行业按需加载 |

---

## 九、工程与构建

```
panorama.html              构建产物，勿直接改
panorama_template.html     界面模板（唯一真实来源），含 /*__DATA__*/ 占位符
build_panorama.py          合并数据 → 算派生字段 → 注入模板

data_ability.py            产业侧：行业/子行业/岗位/能力项 + 三级描述 + 来源证据
panorama_ext.py            扩充岗位与能力项、演示学校数据、构建流水线演示
data_major_standard.py     专业教学标准 + 教学形式规则表
data_courselib.py          课程体系 + 实训条件要求 + 实训条件字典
data_knowledge.py          本校知识体系示例 + 91 条映射边

generate.py / validate.py  Obsidian vault 生成与校验
_历史片段/                  早期分文件开发的 JS，内容已并入模板，仅作参考
```

构建：`python3 build_panorama.py`

---

## 十、待确认事项（跨平台）

| # | 事项 | 背景 | 建议 |
|---|---|---|---|
| 1 | **核心能力口径「与」还是「或」** | 「服务礼仪与职业形象呈现」被 7 个岗位要求、覆盖面最广，却因无国标背书掉出核心集 | 提请图谱平台确认，或拆成通用核心 / 法定核心两类 |
| 2 | **能力覆盖面分层口径** | 「通用 / 共性 / 岗位专属」不在 V2 方案内，「共性」档已隐藏、算法保留 | 在图谱平台定成规则并随图谱产出 |
| 3 | **产业趋势可追溯性** | 无来源证据，只能标注不能核对 | 建议 N3 抽取时记录 `fromTrend` |
| 4 | **人才培养方案解析** | 当前仅说明入口 | 实现后可把输入成本从三分钟压到三十秒 |

---

## 十一、已发现的问题（v1.2 新增）

对照实际构建逐项核实后发现。**B 类为 bug，必须修；O 类为优化建议，可排期。**

### B-1 ⚠️ 未勾选实训资源被判定为「缺」

**现象**：第 4 层是**选填**层。跳过或未勾任何资源时，处方第 4 档直接输出
「缺实训条件 · 导游服务综合实训室 / 急救技能训练器材 / 讲解实训室 / 语言实训室」。

**为什么是 bug**：学校很可能**明明有**导游服务综合实训室，只是还没勾。
系统把「未填报」当成了「确认没有」，给出的是错误结论——而这一档是"要花钱 · 高成本"的建议，
错判会直接误导经费决策。

**应改为**：第 4 层未完成时，该档显示「**本校资源尚未填报，无法比对**」并引导回第 4 层；
只有用户完成第 4 层后，未勾中的才判定为"缺"。这正是 §7 新增的设计原则 9。

### B-2 ⚠️ 依据文件未去重

**现象**：「旅游法与合同条款应用」的依据文件显示为
`中华人民共和国旅游法、中华人民共和国旅游法`。

**原因**：该能力项确有两条证据同源不同摘录（数据本身正常），但 V2 渲染处
`(a.evidences||[]).filter(...).map(e=>e.sourceTitle)` **未按 sourceTitle 去重**。
V1 的对应实现是有 `seen` 集合去重的，重构时丢失了。

**应改为**：按 `sourceTitle` 去重后再拼接。

### O-1 V1 死代码约 1000 行未清理

`const L1 = [..., ['assess','能力测评0.1','hide'], ...]` —— V1 tab 已隐藏，
但 §6b / 7 / 7a / 7b / 7e 的 JS 与相应 CSS 全部保留。

已核实：**V1 段共定义 39 个函数，其中 30 个为纯 V1 自用**，可安全删除；
另有 4 个被 V2 复用（`clearOverlay` `libModules` `setDiag` `toast`），
需先抽出到公共段再删其余。

**影响**：模板从 209 行级增至 4850 行；后续改 bug 时容易改错一套；CSS 类名可能冲突。
**建议**：抽出 4 个共用函数 → 删除 V1 段 → 重跑全流程验证。

### O-2 第 4 层列出全部 10 项资源，其中 6 项与本次处方无关

导游服务专业的处方只涉及 4 项实训条件（`LAB-GUIDE` `EQ-AID` `LAB-SPEAK` `LAB-LANG`），
但界面要求用户在 10 项中查找勾选，其余 6 项（线路成本核算工作台、研学活动模拟实训场地等）
与该专业的缺口无关。

**建议**：**与本次处方相关的排在前面并标出**（如「本次处方涉及」分组），
其余折叠为「其他资源（6 项）」。既降低勾选成本，也避免用户误以为需要全部具备。

### O-3 「岗位支撑度 100%」与「培养深度 3/17」口径打架

同一份报告里，符合度一节显示「地接导游 支撑度 100%」「全陪领队 支撑度 100%」，
而概况总览显示「培养深度 3 / 17」。用户必然会问：**支撑度 100% 了，为什么只有 3 条达标？**

原因是支撑度算的是"有没有课程覆盖"，不含深度。**但界面上没写。**

**建议**：在支撑度旁注明口径（如「支撑度 = 有课程覆盖的比例，不含是否教到岗位要求高度」），
或改称「覆盖度」与"培养深度"形成明确对照。这是本次评审新增的设计原则 10。

### O-4 处方档位编号跳号

实测输出为 `1 现有课加深` → `3 新增课程模块` → `4 新建实训条件`，
档位 2、5 因条件未触发而缺席。**跳号会让用户以为内容有遗漏。**

**建议**：改为连续编号，把五档固定显示、未触发的档位显示为「本次未触发」并说明判定条件；
或去掉序号只保留成本标签。

### O-5 「导出这一页」在第 1 层即出现

第 1 层时报告仅 350 字符（只有概况总览的前半），此时导出意义有限。

**建议**：第 3 层完成后再出现，或在第 1、2 层时改为次要样式并提示「完成第 3 层后内容更完整」。

### O-6 能力雷达"另有 N 条未画"的出口不明确

提示文案为「另有 6 条未画，请切到单岗位查看」，但切到单岗位后该岗位的能力数
仍可能超过 12 条上限，未必能让那 6 条露出来。

**建议**：改为直接提供「展开全部」，或明确说明"完整清单见上方能力要求清单"。

---

## 附录 · 术语表

| 术语 | 含义 |
|---|---|
| 四层能力图谱 | 行业 → 子行业 → 岗位 → 能力项 |
| 知识体系 / 知识图谱 | 专业 → 课程 → 模块 → 任务 → 操作流程 → 知识点 / 技能规范 / 标准 / 案例 |
| 课程体系 | 平台预置、按专业教学标准建的课程与知识点集合 |
| 双图谱映射 | 能力项 ↔ 知识点 / 技能规范之间的关系边 |
| 三级行为化描述 | 能力项在 L1/L2/L3 各级的行为描述、观测点、考核方式 |
| 观测点 | 可直接观察到的学生行为表现，2~4 条 |
| 观测点差集 | 现状等级与目标等级观测点之差，即"要补的教学目标" |
| 服务等级 | 一条映射边把该能力支撑到哪一级 |
| 标准对接岗位 | 专业教学标准中该专业"面向的职业岗位" |
| Gate 1 | 行业-专业配对，即标准岗位 → 能力图谱岗位的映射 |
| 诊断批次 | 一次测评的结果快照 |
