---
node_type: "方案"
name: "双图谱映射方案"
version: "v1.0"
tags:
  - "索引"
  - "方案"
---

# 双图谱融合方案 · 知识体系 × 能力图谱

## 一、要解决的问题

两侧图谱各自跑通了，但它们是两张互不相识的图：

- **能力图谱**（V2 流水线产出）：行业 → 子行业 → 岗位 → 能力项，每条能力带三级行为化描述与来源证据。它知道**岗位要什么**，不知道**哪门课在教**。
- **知识体系**（Dify 工作流产出）：专业 → 课程 → 模块 → 任务 →（操作流程 → 操作步骤）→ 技能规范 / 知识点 / 标准 / 案例。它知道**教了什么**，不知道**为哪个岗位服务**。

缝合这两张图，是为了同时回答四个问题：

| 问题 | 谁问 | 靠什么回答 |
|---|---|---|
| 这门课到底在为哪些岗位能力服务 | 专业负责人 | 课程 ← 叶子 → 能力项 上浮 |
| 这个岗位的能力，我们的课程覆盖了多少、教到几级 | 教务、评估专家 | 岗位 → 能力项 → 覆盖状态 |
| 学生想去这个岗位，该补哪几门课 | 学生、就业指导 | 岗位反推课程 |
| 老师说「我要讲投诉处理」，该调哪些素材 | 教师端工作台 | 自然语言 → 能力项/任务 → 双侧召回 |

## 二、映射锚点选在哪一层

两侧粒度不对等，所以第一个决策是**在哪一层缝**。三种选法的取舍：

| 方案 | 映射条数 | 精度 | 致命问题 |
|---|---|---|---|
| 能力项 ↔ 课程 | 最少 | 最粗 | 只能回答「这门课支撑哪些能力」，无法定位到具体教学内容，生成课件时召回的是整门课 |
| 能力项 ↔ 任务 | 适中 | 中 | 任务是教学单元不是能力单元，一个任务常同时服务多条能力，映射后仍需二次判断 |
| **能力项 ↔ 技能规范 / 知识点** | 最多 | 最细 | 维护成本高——但这正是自动化要解决的问题 |

**选最细的一层**。理由不是"精度高"这么空泛，而是：

1. **只有这一层两侧是同构的。** 能力项的观察点写的是「能做什么、做到什么标准」；技能规范的 `observable_ability_requirement` 写的也是「能识别/能判断/能操作」。这两个字段在语义上是同一类东西，可以直接对齐。而课程名、任务名是教学组织的产物，与能力表述之间隔着一层抽象。
2. **粗粒度可以从细粒度上浮，反过来不行。** 有了叶子级映射，能力项→任务、能力项→课程、岗位→课程全部可以由程序聚合出来，且随数据变化自动更新。先做粗的，将来要细就得推倒重来。
3. **生成课件需要的是叶子。** 教师端工作台按老师输入召回图谱内容时，能用的是知识点的 `core_content`、案例的情景与判断、标准的达标要求——不是课程名。

## 三、观测点锚定法（本方案的核心）

**不要用名称相似度做映射。** 「客诉情绪安抚与共情表达」和「投诉要素倾听与记录」名称不像，但一个是另一个的直接支撑；「服务意识」和「服务规范」名称很像，却毫无支撑关系。

用**行为描述对行为描述**：

```
能力项 ABL-07 · L2 级
  behaviorDesc:      能够独立接待情绪激动的投诉游客，在 5 分钟内完成场景隔离与情绪安抚
  observablePoints:  ① 主动将投诉游客带离团队视线范围
                     ② 使用复述确认对方诉求并获得确认
                     ③ 给出明确的回复时间点而非「尽快」
                             ↕  逐条比对
知识侧 SKS-10 · 技能规范
  observable_ability_requirement: 能通过共情表达与复述确认在 5 分钟内使游客情绪平复
  key_operation_requirements:     禁用「这不归我管」「你先冷静」等表述
  achievement_result:             5 分钟内游客停止情绪化表达并认可处理时限
```

三项观察点与该技能规范逐条对应 → `relation: covers`，`serves_level: L2`，`anchor: 观测点锚定`，`confidence: 0.95`。

这样做的三个好处：

- **可解释。** 每条边都能说出「因为观察点②和 key_operation_requirements 说的是同一件事」，评估专家问起来有话答。
- **可复算。** 观察点和技能规范都是结构化字段，映射可以由程序重跑，不依赖某个人的记忆。
- **等级自然浮现。** 对上的是 L1 观察点还是 L3 观察点，映射时就确定了——这就是下面的 `serves_level`。

**锚定方式分四种**，写进边的 `anchor` 字段：

| anchor | 适用 | 置信度基准 |
|---|---|---|
| 观测点锚定 | 技能规范 / 知识点 ↔ 某级观察点 | 0.90–0.95 |
| 规则援引 | 标准 ↔ 能力项（标准提供达标判据） | 0.95 |
| 名称同义 | 两侧名称就是同一概念的不同说法 | 0.90 |
| 人工指定 | 前两种都不成立，靠业务判断连的边 | ≤ 0.85，必须进人工队列 |

## 四、映射边的五个字段

边不是一根光秃秃的线。它必须带够信息，否则上浮出来的结论没法解释。

```yaml
map_id:       MAP-047
from_kg:      SKS-10 情绪安抚与复述确认        # 知识侧
to_ability:   ABL-07 客诉情绪安抚与共情表达      # 能力侧
relation:     covers          # 关系类型
serves_level: L2              # 服务于能力项的哪一级 ★
coverage:     0.9             # 这一条节点对该级的贡献度
anchor:       观测点锚定       # 怎么连上的
confidence:   0.95            # 置信度，决定是否入人工队列
note:         与 L2「5 分钟内完成场景隔离与情绪安抚」逐字对应
```

**`relation` 四取值**，区分开支撑强度：

| 值 | 含义 | 典型来源 | 计入覆盖度 |
|---|---|---|---|
| `covers` | 直接支撑：行为要求与观察点同构 | 技能规范 | 是 |
| `partially_covers` | 部分支撑：是能力的一块拼图，单独不够 | 知识点 | 是 |
| `prerequisite_of` | 前置铺垫：不构成能力，但缺了建不起来 | 概念类知识点 | 否 |
| `assesses` | 提供达标判据或检验情境 | 标准、案例 | 否 |

不做这个区分，会得到虚高的覆盖率——十个知识点连上一条能力，看起来覆盖得很好，实际一个技能训练都没有。

**`serves_level` 是最有价值的一个字段。** 它把「教什么」和「教到什么程度」分开。同一条能力，课程可能只教到 L2，而岗位要 L3——这个差额靠名称映射永远看不出来，靠 `serves_level` 一减就出来了。**本示例数据中 7 条能力项存在这种等级缺口**，全部集中在 L3（处理非常规情境、优化流程、指导他人），这类要求本来就该由顶岗实习承接，而不是课堂任务。

## 五、边的落盘策略：分级，不是全量建节点

Obsidian 的双链是无属性的，带属性的边只能"边即节点"。但把 91 条边全建成文件，图谱视图会被边节点淹没。所以分两级：

| 边的状态 | 落盘方式 | 理由 |
|---|---|---|
| `confidence ≥ 0.9` 且能力项无 flag | 写进两侧 frontmatter 的 `maps_to` / `maps_to_abilities`，属性写在两侧正文的映射表里 | 轻量，图谱视图有连线，属性可读 |
| `confidence < 0.9`，或能力项是 AI 推断项 / 带 flag | 额外建 `30-映射层/MAP-xxx.md` 独立文件，带对照材料与复核清单 | 需要留痕、需要有人签字确认 |

本示例中 91 条边，12 条进人工队列。这个比例（约 13%）与 V2 方案里 `inferredRate` 的健康值区间是同一个道理——**全自动确认说明阈值定得太松，全部进队列说明锚定方法没起作用**。

## 六、程序算什么，人判断什么

沿用 V2 方案的分工原则：所有计数、聚合、缺口推导交给代码，模型只做语义对齐。

| 字段 | 产出方 |
|---|---|
| `relation` `serves_level` `coverage` `anchor` `note` | LLM（比对观察点与知识侧字段） |
| `confidence` | 代码（按 anchor 类型给基准分，再按字段匹配度调整） |
| `positionCount` `coveredLevels` `coverageStatus` `coverageGap` | 代码（从边聚合） |
| 课程 → 支撑能力项、岗位 → 课程覆盖率 | 代码（沿归属链上浮） |
| 内容缺口 / 等级缺口 / 知识孤儿 | 代码（三类缺口的判定规则见下） |
| 待复核边的最终结论 | 人 |

**三类缺口的判定规则**（全部可由程序算出，无需人工维护）：

```
对每条能力项 A：
    teachEdges = A 的入边中 relation ∈ {covers, partially_covers}
    coveredLevel = max(teachEdges.serves_level)          # 课程教到几级
    requiredLevel = max(要求 A 的各岗位的 requiredLevel)   # 岗位要几级

    teachEdges 为空                        → 内容缺口：课程完全没教
    coveredLevel < requiredLevel           → 等级缺口：教了但不够深
    否则                                   → 已覆盖

对每个知识侧叶子 N：
    N 无出边                               → 知识孤儿：教了但不服务任何岗位能力
```

三类缺口对应三种完全不同的动作：内容缺口要**加课**，等级缺口要**加深度或转实习**，知识孤儿要**补映射或删内容**。混为一谈就没法指导专业建设。

## 七、命名空间与 ID 规则

两侧前缀完全分离，映射层单独一套。这样三份数据可以独立生产、独立版本化，合库时不会撞 ID。

| 前缀 | 节点 | 侧 |
|---|---|---|
| `IND` `SUB` `POS` `ABL` | 行业 / 子行业 / 岗位 / 能力项 | 产业侧 |
| `MAJ` `CRS` `MOD` `TSK` `FLW` `STP` | 专业 / 课程 / 模块 / 任务 / 操作流程 / 操作步骤 | 教育侧 |
| `SKS` `KNP` `STD` `CAS` | 技能规范 / 知识点 / 标准 / 案例 | 教育侧叶子 |
| `MAP` | 映射边 | 映射层 |

文件名为 `ID 名称.md`，ID 在前保证排序稳定与同名不冲突，名称在后保证双链可读。

## 八、多专业多行业时怎么长

映射是**多对多**的，这一点从一开始就要认：

- 一个专业可能面向多个岗位（导游服务 → 地接导游 + 景区讲解员）
- 一个岗位可能被多个专业培养（景区讲解员 ← 导游服务 + 文物修复与保护）
- 一条能力项可能被多个专业的多门课支撑（服务礼仪几乎每个专业都有）

所以入库以**扁平结构**为准：`能力字典` + `知识节点表` + `映射边表` 三张表。树形（本仓库的目录结构）是它的展示投影，不存可写副本——这一条与 V2 方案第十六节的结论一致。

跨专业合库时，映射边表自然聚合，一条能力项的 `supporting_courses` 会从一门变成多门，覆盖率随之上升。这正是"多个专业共同支撑一个岗位"的真实写照。

## 九、增量维护

课程改版、能力图谱重跑，是常态。三条规则让改动不至于失控：

1. **两侧本体独立演进。** 改课程不动能力图谱，重跑行业不动课程体系。
2. **改动只落在边上。** 课程加深了一个任务 → 改对应边的 `serves_level`，不改任何一侧的节点。
3. **节点删除时边级联标记而非直接删。** 被删节点的边标 `orphaned`，进人工队列决定重连还是废弃——直接删边会悄无声息地让覆盖率虚高。
