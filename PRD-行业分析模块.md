# PRD · 行业分析模块

**产品**　AIGC 智慧课堂教学平台 · 管理端
**模块**　功能模块 1「行业分析」
**状态**　开发版　|　参考原型：`panorama.html`（旅游业 v1.3 示例数据）
**日期**　2026-09-14

---

## 一、概述

### 1.1 业务背景

职业院校每年都要回答四个问题，而这四个问题目前都缺少可核对的产业侧依据：

| 学校要做的事 | 现状痛点 |
|---|---|
| 专业设置动态调整（报什么、撤什么） | 依据来自经验与零散调研，写不出可追溯的论证 |
| 人才培养方案修订（加哪门课、砍哪门课） | 不知道产业实际要什么能力、要到什么深度 |
| 实训条件建设经费投向 | 缺少"为什么要买这台设备"的能力级依据 |
| 双高 / 提质培优 / 产教融合共同体申报 | "对接产业需求"一章无实证材料 |

四件事有个共同点：**结论都要拿给别人看**（给校长批经费、给评审专家论证）。
因此本模块的产出不只是"能在线浏览"，而必须**能带走、能被核对**。

### 1.2 模块定位与平台分工

| | 图谱平台（独立平台） | 本模块 |
|---|---|---|
| 职责 | **造图并确认** | **用图** |
| 用户 | 业务方 / 行业专家 | 校领导 / 专业负责人 / 教务 |
| 是否需要拍板 | 需要，且在那边一次性定完 | **不需要** |
| 数据方向 | 产出并发布 | 只读消费，不回写 |

**这是贯穿全模块的硬约束**：凡是需要人判断的事都在图谱平台完成并随图谱入库；
本模块不得出现任何"待确认""待拍板"的数据。
唯一例外是用户描述自身情况的输入（选专业、选岗位、勾课程、勾资源）——那是他自己的事实。

### 1.3 在产品中的位置

```
AIGC 智慧课堂教学平台
├─ 教师端工作台        备课、生成课件教案（另有 PRD）
└─ 管理端
    ├─ 数据概览
    ├─ 行业分析        ← 本 PRD
    ├─ 教务基础数据    （实训室清单在此维护，本模块只读）
    ├─ 课程计划管理    （学时、开课学期在此决策，本模块不生成）
    ├─ 账号与角色
    └─ 系统配置
```

---

## 二、用户与场景

### 2.1 目标用户

| 角色 | 关心什么 | 主要动作 |
|---|---|---|
| 校领导 / 分管教学副校长 | 专业布局对不对得上产业 | 看行业全景 → 看概况总览 → 导出这一页 |
| 专业负责人 / 系主任 | 缺什么能力、课怎么改 | 走四层测评 → 看处方（按能力视图） |
| 教务处 | 补课占多少课时、排哪学期 | 看处方（按课程视图）→ 转入课程计划管理 |
| 企业方 / 评估专家（只读） | 这份数据可不可信 | 看数据可信度、点开来源证据 |

### 2.2 核心场景

| 编号 | 场景 | 输入成本 |
|---|---|---|
| S1 | 了解一个行业的岗位与能力全景 | 零输入 |
| S2 | 对本校某专业做一次体检 | 四层渐进，随时可停 |
| S3 | 把结论带走（开会、修方案、写申报材料） | — |
| S4 | 核对任一结论的依据 | — |

### 2.3 非目标

- 学生与家长（本模块不面向招生宣传）
- **同类院校横向对比**——无数据来源，不做，且不得以任何形式暗示具备该能力

### 2.4 「差距」的基准

「我跟市场的差距」中的"市场"有三个可能基准，本模块的取舍：

| 基准 | 提供 | 说明 |
|---|---|---|
| 产业岗位要求（能力图谱） | ✅ | 本模块本行 |
| 国家专业教学标准（应教什么） | ✅ | 学校最在意的硬约束，是被检查的对象 |
| 同类院校水平 | ❌ | 无数据 |

两者叠加才有说服力：「这几条国家标准里没写，但多个岗位在招聘时都要」——
既是新信息又有依据，正是申报材料需要的素材。

---

## 三、功能范围

### 3.1 本期范围

| 分组 | 功能 |
|---|---|
| 行业全景 | 行业检索与切换、四层能力全景（环形 / 树形 / 卡片）、能力矩阵、产业趋势与新兴能力、数据口径与可信度说明 |
| 能力测评 | 四层渐进式输入、概况总览、能力要求清单、能力雷达图、诊断结论、调整建议（五档 · 双视图）、能力提升路径卡 |
| 通用 | 诊断结果叠加回行业全景、版本历史、Markdown 导出、打印视图、图谱平台构建流水线演示入口 |

### 3.2 明确不做

| 不做 | 原因 |
|---|---|
| 同类院校横向对比 | 无数据 |
| 学时 / 开课学期 / 课程名的自动生成 | 图谱里无依据，属教务决策范畴 |
| 人才培养方案真实解析 | 本期仅保留入口，见 F-46 |
| 行业图谱的构建与编辑 | 归图谱平台 |
| 多专业群组群合理性评估 | 需先打通专业群界定规则 |
| 逐条询问"这门课教到什么程度"的填报 | 教师说不准，得到的是噪音，见设计原则 3 |

---

## 四、信息架构

```
行业分析
├─ 顶部　行业输入框（通栏）+「分析」按钮
│
├─ 一级 tab：行业全景
│   ├─ 能力图谱      环形 / 树形（默认）/ 卡片
│   ├─ 能力矩阵      岗位 × 能力项
│   └─ 趋势与新兴    产业趋势 + 新兴能力项
│
├─ 一级 tab：能力测评
│   ├─ 常驻头条      专业 · 时间 · 第 N / 4 层 · 面向岗位数 · 保留课数 ｜ 叠加 · 历史 · 导出
│   ├─ 第 1 层  选专业
│   ├─ 第 2 层  选择目标岗位
│   ├─ 第 3 层  选择已设课程
│   ├─ 第 4 层  选择已有资源（选填）
│   └─ 诊断报告
│       ├─ 概况总览            三个分层数 + 人话结论 +「导出这一页」
│       ├─ 能力要求清单
│       ├─ 能力雷达图          可按岗位切换
│       ├─ 诊断结论
│       └─ 调整建议            五档 ｜ 按课程 / 按能力
│
└─ 页脚说明标签
    数据时效 · 行业边界说明 · 排除项 · 纳入范围 · 数据可信度
```

---

## 五、功能详述

### 5.1 行业检索

**F-01 · 输入框**

- 通栏输入框 + 右侧主按钮「分析」，回车等同于点击
- 聚焦时下拉列出**已发布的行业**，每行只显示行业名
- 匹配行业名与别名（输「文旅」命中「旅游业」）
- **未发布的行业不出现在下拉中**——避免用户点进死胡同

**F-02 · 分析动作**

| 结果 | 行为 |
|---|---|
| 命中 | 加载该行业全景、切到「行业全景」、滚到顶部，toast 提示加载的数据规模 |
| 未命中 | toast「没有找到该行业的图谱。图谱在图谱平台建成并发布后会自动出现在这里。」输入框保持原值 |
| 输入为空 | 同"未命中" |

**F-03 · 未建图行业**

输入未发布的行业名后点「分析」→ 打开「尚未建图」抽屉，说明平台分工；
抽屉内提供次要按钮进入图谱平台构建流水线演示（F-90）。

---

### 5.2 四层能力全景

**F-10 · 三种展示形态**（切换器在卡片右上角；切换时右侧统计栏保持可见）

| 形态 | 适用 | 要点 |
|---|---|---|
| 环形（旭日图） | 看整体结构与体量 | 四层：行业 → 子行业 → 岗位 → 能力要求；外环每格 = 一条岗位能力要求，色 = 要求等级 |
| 树形（**默认**） | 追溯关系 | 点子行业 / 岗位向右追出关联能力；点能力项向左反推关联岗位与子行业，双向高亮 |
| 卡片 | 逐岗位浏览 | 按子行业分组的岗位卡片 |

**F-11 · 右侧统计栏**（三形态下常驻）

- 四项计数：子行业（已建图 / 已识别）、代表岗位、能力项（收敛于 N 个领域）、核心能力
- 岗位能力强度排序：按 L3 项数降序，条形按 L1/L2/L3 分段

**F-12 · 未纳入本期范围的子行业**

环形图中以斜纹空楔形表示，标注「未纳入本期」。
**不得显示为"待拍板"**——纳入范围是图谱平台已确认的结论。

**F-13 · 详情抽屉**

| 对象 | 内容 |
|---|---|
| 能力项 | 定义 → 为什么是核心能力（判定口径 + 本条实际数值）→ 本校支撑情况（F-65）→ 三级行为化描述（每级：行为描述、2~4 条观测点、考核方式）→ 被哪些岗位要求 → 来源证据（标题、等级、原文片段、链接） |
| 岗位 | 别名、职责、职业层级、能力清单（按权重排序，标注要求等级） |

---

### 5.3 能力矩阵

**F-20** 岗位 × 能力项二维表，格子色 = 该岗位对该能力的要求等级，空白 = 不要求。

- 列按子行业分组，行按能力领域分组；表头与首列吸附
- tab 计数显示为**维度** `9 × 28`；页内说明首句给出"其中 103 个格子有要求"
- 上方附能力领域分布条形图

---

### 5.4 趋势与新兴

**F-30 · 产业趋势**

标题旁 ⓘ 悬停/点击显示说明。**必须如实标注**：这些由建图模型基于公开信息给出，
**无来源证据、未经原文校验**，作用是提示新兴能力方向，请当线索不当依据。
视觉上以虚线框、次要权重呈现，与有证据的能力项区分开。

**F-31 · 新兴能力项**

国家标准尚未收录、仅有市场侧来源，已由行业专家在图谱平台确认后纳入。
每条可点开查看来源证据与三级行为化描述。标题旁 ⓘ 说明这批为什么值得优先看。

---

### 5.5 能力测评 · 四层渐进式

#### 5.5.0 设计范式

**系统先按国家专业教学标准摆出"标准答案"，用户只做减法与修正。**

> 「按《导游服务》专业教学标准，这个专业面向这 3 个岗位、开这 4 门课。哪些你们没有？」

人对"改一份现成的"远比"填一张空表"有耐心；且用户还没输入任何东西，
就已经看到了标准答案、已经开始对照了。

#### 5.5.1 分层结构

| 层 | 名称 | 必填 | 输入成本 | 解锁 |
|---|---|---|---|---|
| 1 | 选专业 | 是 | 一次点击 | 岗位面向（第一个数） |
| 2 | 选择目标岗位 | 是 | 半分钟 | 能力要求清单与优先级 |
| 3 | 选择已设课程 | 是 | 三分钟 | 内容完整 / 培养深度（第二、三个数）、雷达图、诊断结论、调整建议 |
| 4 | 选择已有资源 | **否** | 选填 | 调整建议中的实训条件比对 |

**F-40 · 分步交互规则**

| 规则 | 说明 |
|---|---|
| 步骤卡三态 | `active` 进行中 / `done` 已完成（收起为一行摘要）/ `locked` 待办 |
| 完成态摘要 | 显示本层结果 + 「已解锁：×××」 |
| 常驻头条 | 随层级实时更新：`专业名 ｜ 时间 ｜ 第 N / 4 层 · 面向 M 个岗位 · 保留 K 门课` |
| **每层都有完整报告** | 每完成一层，报告区立刻重算并追加新的一节，不是填完才给 |
| 可回退 | 已完成的层可重新展开修改 |
| 修改上层 | 报告**保留**并出现变更提示条 +「按当前设置重算」，**不静默清空** |

#### 5.5.2 第 1 层 · 选专业

**F-41** 卡片式陈列平台已收录的专业，每张显示：专业名、层次 · 代码、培养目标。

- **支持多选**（学校有多个相关专业时，报告按专业分节）
- 提供「全选 / 清空」
- 旁置次要入口「上传人才培养方案（快速通道）」，见 F-46
- 底部主按钮「确认专业，继续」

选定后即可算出**岗位面向**：标准对接岗位数 / 行业岗位总数。

**F-42 · 标准对接岗位的可核对性**

专业教学标准里的岗位名与能力图谱的岗位 ID 是两套命名，其映射（Gate 1）必须是**显式数据**，
不允许运行时猜测。报告中必须同时呈现：

```
地接导游　支撑度 100%
标准原文「地陪导游」· exact 精确匹配
```

匹配方式取值：`exact` 精确 / `近义` / `归并`。

#### 5.5.3 第 2 层 · 选择目标岗位

**F-43** 标准对接岗位**预选**，用户可增（"我们还面向研学指导师"）可减（"我们不做计调"）。

这一步把报告从"通用"变成"你们的"。

#### 5.5.4 第 3 层 · 选择已设课程

**F-44 · 预勾选状态为"标准课程表全部已开"**，用户动作是**取消**没开的课。

| 类别 | 默认 | 标记 |
|---|---|---|
| 该专业标准课程表内的课 | **已勾** | 标准课程 |
| 课程体系内、不在该专业标准课程表里的课 | 未勾 | 拓展课程 |
| 学校自有、标准外的课 | — | 可从课程体系搜索添加 |
| 标准要求但课程体系无对应课程（`coursesUnmapped`） | 单列 | 不参与计算 |

- 每门课可展开，勾到**知识点 / 技能规范**一级（对应"这门课开了但没上第 3 个模块"）
- 课程行三态：未勾 / 部分勾（`–`）/ 全勾（`✓`）
- **不显示学时与开课学期**——平台课程体系不掌握某校的排课，无数据来源

> **不得询问"这门课教到什么程度"。** 达成等级一律由课程内已勾知识节点沿映射边推出。

#### 5.5.5 第 4 层 · 选择已有资源（选填）

**F-45** 从平台实训条件字典中勾选本校已有的资源。

**排序要求**：**与本次处方相关的资源排在最前并分组标出**（如「本次处方涉及（4 项）」），
其余折叠为「其他资源（N 项）」。不得让用户在全量清单里自行查找。

**未填报的处理（重要）**：第 4 层未完成时，第 4 档必须显示
「**本校资源尚未填报，无法比对**」并提供回到第 4 层的入口。
**不得把"未填报"判定为"缺"**——该档指向的是经费决策，错判会直接误导。
只有用户完成第 4 层后，未勾中的项才判定为"缺"。

**F-45.1 · 清单的取值范围必须说清**

该清单当前是**课程体系中所有课程提出过的实训要求的并集**，不是本校资源台账。由此：

- 它会列出与本次所选专业无关的项（实测只选导游服务专业时，10 项中 6 项来自未选的课）
- 它也漏掉本校实际拥有、但课程体系未提及的资源

因此：

1. 清单应**收敛到本次目标岗位所需能力在目标等级上要求的条件**，而非全量并集
2. 必须提供「**以上都没有 / 我还有别的**」的自由输入口，作为反馈给图谱平台补建字典的入口
3. 勾选项旁显示 `spec` 规格供用户自行判断，**不得因勾选即判定规格达标**
4. 本校资源的唯一真值来源是**用户在本层的勾选**；数据文件中任何预置的"本校已有"清单
   均不得参与判定，也不得用作默认勾选

#### 5.5.6 快速通道

**F-46 · 上传人才培养方案**

学校一定有这份文件，其中培养目标、就业岗位、课程体系表齐全，解析后可直接跳到第 4 层。

**本期仅做说明入口**，说明正式版将解析出上述内容供用户确认。
**必须明确标注为本期未实现**，不得做假的解析动画或假的进度条。

#### 5.5.7 空态与异常

| 情形 | 表现 |
|---|---|
| 未选专业 | 第 2~4 层为 `locked`，报告区为空，不显示空壳标题 |
| 第 2 层清空全部岗位 | 「确认」按钮置灰，提示真实原因「请至少选择一个目标岗位」 |
| 第 3 层未勾任何课程 | 允许继续，报告显示"本校暂无课程承接"，全部能力项为内容缺口 |
| 该专业无标准课程表数据 | 第 3 层退化为课程体系全量列表（默认全不勾），并提示"该专业暂无标准课程表，需向图谱平台反馈补建" |
| 行业图谱加载失败 | 整页错误态，给出重试；不显示半截数据 |

---

### 5.6 诊断报告

#### 5.6.0 AI 介入边界（适用于报告全部章节）

**F-49 · 报告正文不调用模型**

所有结论、排序、分档均为规则生成，以保证同一批数据任意次运行输出一致——
这份报告会进专业论证会与申报材料，**可复现是硬要求**。

允许 AI 介入的位置，**共同前提是"输出停在候选状态，经用户确认后才进入报告"**：

| 位置 | 说明 |
|---|---|
| 快速通道解析人才培养方案 | 抽取培养目标 / 就业岗位 / 课程体系表 → 预填四层 → **用户确认后**方可生成报告 |
| 图谱平台侧的候选边生成 | 如实训条件边，由考核方式与观测点生成候选，经证据校验与人工闸口后入库 |
| 观测点改写为课程标准句式 | **用户主动触发**的旁路输出，独立展示并标注「AI 改写，原文见上」，不进报告正文 |

明确不允许：让模型撰写或润色结论、决定调整建议的优先级、补充任何凭空生成的文字。

**F-49.1 · 三个共用的中间量**

下游几乎所有章节都建立在它们之上，口径错一处全篇皆错，因此单列并统一实现。

| 中间量 | 输入 | 口径 |
|---|---|---|
| **要求等级表** `reqMap` | `positions[].abilities[].{ability, level}`，范围由第 2 层所选岗位限定 | 同一能力被多个目标岗位要求时，**取最高等级**——"市场要你做到什么程度" |
| **达成等级表** `att` | `courseLib[].edges[].{ability, leaf, level, rel}`，仅计入第 3 层已勾选的 `leaf`，且 `rel ∈ {covers, partially_covers}` | 同一能力被多条边指向时，**取最高等级**——"你现在教到什么程度"。<br>⚠️ 现行实现取的是首条命中边，导致培养深度被系统性低估，**上线前必须修正** |
| **缺口清单** `gapList` | 上两者 | `att < req` 的能力项。`att = 0` 为**内容缺口**，`0 < att < req` 为**深度缺口**。排序：核心能力优先 → 被要求岗位数次之。**按课程 / 按能力两个视图共用此排序**，切换时相对位置不变 |

#### 5.6.1 概况总览

**F-50 · 不给单一"体检分"**

分数没有可比基准（跟谁比？），且会掩盖结构差异——两所学校同样 60 分，
一个是面向窄、一个是教得浅，**对策完全相反**。

**F-51 · 三个分层的数**

| 层 | 显示格式 | 口径 |
|---|---|---|
| 岗位面向 | `3 / 9`　市场 9 个岗位，你面向 3 个 | 目标岗位数 / 行业岗位总数 |
| 内容完整 | `17 / 18`　面向的岗位要 18 条能力，你覆盖 17 条 | 有任一支撑的能力项 / 目标岗位要求的去重能力项 |
| 培养深度 | `3 / 17`　覆盖的 17 条里，达到岗位要求高度的 3 条 | 达标能力项 / 有支撑的能力项 |

**卡片内不写「对应决策」文案。** 三个数与五档处方的对应关系并非一一映射——
深度缺口首先落在第 1 档（改教学设计），只有目标 L3 的部分才涉及实训；
把它标成"要不要加实训（花钱）"会与 F-59 的档位直接冲突。

**F-52 · 人话结论**

三个数下方一句结论，**必须由明确规则生成**，阈值写成常量并注释，不在运行时调模型。

```
COVERAGE_OK   0.70    覆盖率 ≥ 此值 → 视为「教到了」
ATTAIN_OK     0.50    达标率 ≥ 此值 → 视为深度足够
COVERAGE_LOW  0.50
POS_NARROW    0.40    岗位面向 < 此值 → 面向偏窄
CORE_GAP_MIN  3       核心能力缺口 ≥ 此值 → 整块内容没有承接
POS_COV_LOW   0.40    单岗位覆盖率 < 此值 → 触发处方第五档
```

| 条件 | 结论句式 |
|---|---|
| 覆盖率 ≥ `COVERAGE_OK` 且 达标率 < `ATTAIN_OK` | 「问题不在『没教』，在『教得浅』」 |
| 覆盖率 < `COVERAGE_LOW` 且 核心能力缺口 ≥ `CORE_GAP_MIN` | 「有整块内容没有课程承接」 |
| 岗位面向 < `POS_NARROW` | 「专业面向偏窄，不是教学质量问题」 |
| 都不触发 | 描述性兜底句，**不硬凑结论** |

**F-53 · 落脚点为结构导向**

先说清能力分布长什么样（「核心优势集中在讲解与表达、服务与沟通方向」），再指出差距。
**不上来就说"覆盖率偏低"**——一所专做研学方向的学校覆盖率 40% 可能完全合理。

**F-54 ·「导出这一页」**

单独按钮，走打印视图，输出概况总览一页。
**第 3 层完成前该按钮为次要样式**，并提示"完成第 3 层后内容更完整"。

#### 5.6.2 能力要求清单

**F-55** 按核心 → 被要求岗位数排序。每条显示：名称、★核心、类型（知识/技能/素养）、
`现状等级 → 目标等级`、被几个岗位要求。

**必须提供图例**说明 `L1 → L2` 的含义（左为本校现状，右为岗位要求），
已达标的显示为 `已达标 L2`，无任何支撑的显示为 `未覆盖`。

#### 5.6.3 能力雷达图

**F-56**

- 轴 = 单条能力项；灰虚线 = 目标岗位要求的最高等级；紫块 = 本校达成；红点 = 缺口轴
- 支持「全部 / 各目标岗位」切换
- 轴按核心优先、要求等级从高到低取前 12 条
- 超出 12 条时，**提供「展开全部」出口**；若不展开，提示文案须指向真实可达的位置
  （如"完整清单见上方能力要求清单"），不得给出无效指引

#### 5.6.4 诊断结论

**F-58** 按内容缺口 / 深度缺口分类陈述，说明最需要补的是哪几条、是否含核心能力、
以及"不是调整现有课程能解决的"这类性质判断。

#### 5.6.5 调整建议 · 五档 · 双视图

**F-59 · 五档排序即回答"先做哪一件"**

| 序 | 档位 | 判定 |
|---|---|---|
| 1 | 现有课加深 | 已开课程覆盖但没教到岗位要求高度，且目标 ≤L2 |
| 2 | 现有课扩内容 | 已开课程内有未勾选的知识节点能补上缺口 |
| 3 | 新增课程模块 | 课程体系里有课能覆盖，但本校没开 |
| 4 | 新建实训条件 | 第 3 档推荐的新开课、或承接了缺口能力的已开课有实训要求，而本校无对应实训室 |
| 5 | 新增专业方向 | 单岗位覆盖率 < `POS_COV_LOW` |

**排序本身即成本梯度**，界面上**不再逐档标注成本文案**——档位顺序已表达"先做哪一件"，
再加"零成本 / 要花钱"这类标签反而容易与概况总览的三个数形成口径冲突。

**编号规则**：五档**固定显示、连续编号**；未触发的档位显示为「本次未触发」并说明判定条件。
不得跳号（如 1 → 3 → 4），跳号会让用户以为内容有遗漏。

**目标等级为 L3 的缺口**单独标注「需实训 / 顶岗承接」——
L3 定义为"处理非常规情境、优化流程、指导他人"，课堂任务难以达成。

**F-60 · 每条建议的内容与出处**

| 内容 | 取自 |
|---|---|
| 承接形态 | 上表规则（卡片内写明判定） |
| 要教到什么程度 / 达标考核 | 能力项三级行为化描述，**原文照搬，不改写措辞** |
| 承接课 / 要补的知识节点 | 课程体系映射边 |
| 无现成课程时 | 必须显示「课程体系无现成课程 · 校本开发」，**不得渲染成一条没有下文的缺口** |
| 实训条件 | 实训条件要求 × 本校已有资源比对（未填报时见 F-45） |

**不生成**：学时、开课学期、课程名、设备型号。卡片说明写明这些"在《课程计划管理》里由教务决定"。

**F-61 · 双视图切换**

| 视图 | 面向 | 组织方式 |
|---|---|---|
| 按课程（默认） | 教务 | 按承接形态与课程分组 |
| 按能力 | 专业带头人 | 能力提升路径卡，见 F-62 |

两视图排序保持一致（核心优先、被要求岗位数次之），用户切换时不迷失。

#### 5.6.6 能力提升路径卡

**F-62** 核心思路：**「怎么提升」= 三级行为化描述中，现状等级与目标等级之间的观测点差集。**

每张卡五块：

| 块 | 内容 | 出处与要求 |
|---|---|---|
| ① 差在哪 | 「现在能做到 · L1」vs「**还要能做到 · L2（要补的教学目标）**」 | `levels[].points` 的差集，**原样取用不改写**；「还要能做到」视觉突出 |
| ② 在哪教 | 课程 › 模块 › 知识点 | 课程映射边；无支撑时明说并指向补课建议 |
| ③ 怎么教 | 考核方式原文 + 教学形式提示 + 可展开「这条建议按哪条规则推出」 | 规则表，见 F-63 |
| ④ 需要什么条件 | 实训条件比对，或「承接课程无实训条件要求」 | 实训条件要求，挂载粒度见 F-62.1 |
| ⑤ 先补什么 | 前置能力；前置本身也在缺口时标出「前置也在缺口」并给依据 | `prerequisites` |

排序与「按课程」视图共用 `gapList`，两视图切换时同一条能力相对位置不变。

**内容缺口（从 0 开始）须分级递进呈现**：先 L1 要达到什么 → 再 L2 → 再 L3，
并标出目标停在哪一级、哪几级课堂达不到。不得一步给到 L3。

**F-62.1 · 实训条件的挂载粒度**

实训条件当前挂在**课程**上，导致同一门课承接的多条能力显示相同条件——
实测一门课承接 11 条能力时，11 张路径卡的 ④ 显示同一套内容，用户翻两张就会发现是复读。

目标形态：条件挂在**「能力项 × 目标等级」**上。同一条能力，加深到 L2 与加深到 L3
所需的条件不同（L1 认知级往往不需要任何实训条件），这一区分正是第 1 档与第 4 档的分界。

锚定方法（在图谱平台建边，教学平台只读）：

1. **`levels.L{n}.assess` 考核方式原文** —— 考核要在什么环境里做，就需要什么条件（最硬的锚点）
2. **`levels.L{n}.points` 观测点里的可量化要求** —— 决定设备**规格**
   （如观测点「按压深度 5–6 厘米、频率 100–120 次/分」直接推出"模拟人须带深度与频率反馈"）
3. **专业教学标准的「实训实习环境」条目** —— 正向校验该条是否有国家级依据

每条边须带四个字段：

| 字段 | 作用 |
|---|---|
| `anchor` | 这条条件从哪句考核方式 / 观测点推出，可核对 |
| `kind` | 场地 / 设备 / 软件 / 耗材——一套报价单模板与一间 40 工位实训室不是同一量级，不得混列 |
| `spec` | 规格要求。比对时**只判"有没有"，规格须标为待核对**，不得因勾选即判定"已具备" |
| `source` | 依据出处，显示到具体文件名 |
| `substitutable` | 能否由校企共建或顶岗承接替代——第 4 档真正要回答的是"非建不可吗" |

**F-63 · 教学形式提示规则表**

这是全模块**唯一一处系统生成的判断**，因此必须：

1. 规则写在**数据层**（`ASSESS_RULES`），不硬编在前端逻辑中
2. 界面可展开看命中了哪条规则、理由是什么
3. 显式标注「**由考核方式按规则反推，非图谱原生数据**」
4. 命中多条就都显示；**一条都不命中时不硬凑**，输出
   「该考核方式未命中已有规则，教学形式由任课教师判断。」

规则结构：`{keys: [考核方式关键词], hint: 教学形式提示, why: 理由}`

#### 5.6.7 精度边界声明

**F-64** 报告顶部给出总述：

> 「因为还没有你们的实际教学数据，这部分只能给到课程级；接入本校知识图谱后可精确到知识点级。」

各节的精度边界不同（如"能力要求清单"不依赖本校数据、无此限制），**不得一句话通贴全文**；
下列几条必须在对应节就近说明，它们不是缺陷备注，是用户有权知道的口径边界：

| 所在节 | 必须说明的内容 |
|---|---|
| 内容完整 / 培养深度 | 达成等级取自映射边的**最高**等级；若实现为首条命中边，培养深度会被系统性低估 |
| 概况总览三卡 | 「内容完整」只问教没教、「培养深度」才问教多深；三张卡不得标注与 F-59 档位成本相冲突的决策文案 |
| 能力要求清单 ·「N 个岗位要求」 | 该数为**全行业口径**，不是本次目标岗位口径 |
| 能力雷达图 | 轴有数量上限；被裁掉的能力项须指向**真能看到它们的地方**（能力要求清单是全量的），不得指向同样有上限的单岗位视图 |
| 第 4 档 / 路径卡 ④ | 实训条件的挂载粒度与规格核对说明，见 F-62.1 |
| 第 4 层「选择已有资源」 | 该清单是**课程体系中所有课程提出过的实训要求的并集**，不是本校资源台账，见 F-45.1 |

---

### 5.7 本校支撑情况下钻

**F-65** 能力项详情抽屉内，显示该能力由哪门课的哪个知识节点支撑、服务到什么等级。

**仅在已生成诊断且已叠加时显示**；未做测评或未叠加时**整块不出现**——
行业全景须保持为不含任何院校信息的产业侧数据。

---

### 5.8 叠加、版本与导出

**F-70 · 叠加到行业全景**

- 叠加**绑定到产出它的那一版报告**（版本快照），而非"当前状态"
- 横幅写明批次与来源；当前设置已变更时追加「叠加的仍是这一版」
- 取消叠加须有明确反馈，说明全景已恢复为不含院校信息的原始数据
- 叠加标记：环形图岗位红框、树形图能力节点红虚线框、能力矩阵未覆盖行斜纹底

**F-71 · 版本历史** — 会话内历次诊断，含批次、层级完成度、专业、岗位范围、缺口数

**F-72 · 导出**

- **Markdown** —— 含概况总览、能力要求清单、诊断结论、调整建议明细、提升路径
- **打印视图** —— 隐藏导航与交互控件，正文完整保留
- 两者顶部均标明诊断批次号、生成时间、所依据的图谱版本与专业教学标准文件名，使报告可复现

---

### 5.9 说明信息（页脚）

**F-80** 五个标签，悬停或点击展开：

| 标签 | 内容 |
|---|---|
| 数据时效 | 更新时间（含距今）、版本、覆盖范围、行业口径、数据依据、维护方 |
| 行业边界说明 | 边界原则原文 |
| 排除项 | 每条排除项及其理由（理由须指向岗位能力差异） |
| 纳入范围 | 纳入建图的业务方向 |
| 数据可信度 | 来源等级分布、校验指标、说明 |

---

### 5.10 图谱平台构建流水线演示

**F-90** 收在「尚未建图」抽屉的次要按钮后，正常使用路径碰不到。

遮罩层顶部标注「图谱平台 · 构建流水线」，闸口文案明确
「由业务方在图谱平台拍板，这些判断一次定好并随图谱入库，之后行业分析的使用者不再需要碰」。

---

## 六、数据契约

### 6.1 数据来源与归属

| 数据 | 归属 | 本模块权限 |
|---|---|---|
| 行业能力图谱（四层 + 关系 + 证据） | 图谱平台，版本化发布 | 只读 |
| 专业教学标准 | 图谱平台结构化维护 | 只读 |
| 平台课程体系（课程 → 知识点 / 技能规范 + 映射边 + 实训条件要求） | 图谱平台 | 只读 |
| 实训条件字典 | 图谱平台 | 只读 |
| 本校知识图谱（已接入学校） | 学校侧 | 只读 |
| 用户的测评输入与诊断批次（含第 4 层的本校资源勾选） | 本模块 | 读写 |

**本模块不回写任何图谱数据。**

**本校已有资源的唯一真值来源是用户在第 4 层的勾选。** 数据文件或教务基础数据中任何
预置的"本校实训室清单"均不得参与"已具备 / 缺"的判定——否则同一件事会有两套结论。

### 6.2 专业教学标准结构

```jsonc
{
  "code": "740102",
  "name": "导游服务",
  "stage": "中职",
  "years": 3,
  "source": "中等职业学校导游服务专业教学标准（2022 年修订）",   // 必填，可核对
  "goal":   "培养面向……的高素质技术技能人才。",
  "positions": [                        // Gate 1 的显式结果，不允许运行时推断
    { "id": "POS-01", "stdText": "地陪导游",    "match": "exact" },
    { "id": "POS-04", "stdText": "全陪导游、领队", "match": "exact" },
    { "id": "POS-03", "stdText": "景区讲解员",   "match": "近义" }
  ],
  "courses": ["CRS-01", "CRS-02", "CRS-03", "CRS-07"],
  "coursesUnmapped": ["旅游政策与法规"]   // 标准要求但课程体系暂无，如实列出，不硬凑
}
```

**硬性要求**

1. `stdText` + `match` 必填——报告要显示"标准写的是 X，我们对到了岗位 Y"
2. `coursesUnmapped` 如实列出，**不为了好看塞给某门课**
3. `source` 必填，指向具体标准文件名

### 6.3 教学形式规则表结构

```jsonc
[
  { "keys": ["沙盘","演练","模拟","情境"],
    "hint": "需要情境模拟环节",
    "why":  "这类考核无法用笔试达成，必须让学生在拟真情境中做出反应" }
]
```

### 6.4 接口需求（供后端设计参考）

| 接口 | 用途 | 关键返回 |
|---|---|---|
| 行业列表 | 输入框下拉 | 行业名、别名、版本、发布时间 |
| 行业图谱全量 | 行业全景与测评 | 四层结构 + 能力字典 + 关系 + 证据 + 统计 |
| 专业教学标准列表 | 第 1 层 | 见 6.2 |
| 课程体系 | 第 3 层与处方 | 课程、知识节点、映射边、实训条件要求 |
| 实训条件字典 | 第 4 层 | 条件键、名称、规格、依据 |
| 本校已有资源 | 第 4 层预填 | 从教务基础数据拉取 |
| 诊断批次读写 | 版本历史 | 批次快照 |

**性能要求**：图谱数据当前随页面下发（示例数据 116 KB）。
正式版应改为**按行业按需加载**，并对能力字典、映射边做分段拉取。

---

## 七、口径定义

开发实现时以此表为准，**界面上凡并列出现的指标必须各自写明口径**。

| 口径 | 定义 |
|---|---|
| 能力等级 | L1 认知/辅助级、L2 独立操作级、L3 综合应用级 |
| 核心能力 | 被 2 个以上岗位要求 **且** 至少一条国家级来源（口径由图谱平台产出，本模块只读） |
| 达成等级 | 已勾选知识节点沿 `covers` / `partially_covers` 边推出的最高服务等级 |
| 达标 | 达成等级 ≥ 该岗位对该能力的要求等级 |
| 内容缺口 | 无任何支撑 |
| 深度缺口 | 有支撑但达成等级 < 要求等级 |
| 岗位面向 | 目标岗位数 / 行业岗位总数 |
| 内容完整 | 有任一支撑的能力项数 / 目标岗位要求的去重能力项数 |
| 培养深度 | 达标能力项数 / 有支撑的能力项数 |
| 岗位支撑度 | 该岗位要求的能力中**有课程覆盖**的比例（**不含深度**）；本期仅用于第 5 档判定与 Gate 1 展示 |
| 观测点差集 | 目标等级 `points` 减去现状等级 `points`，即"要补的教学目标" |

---

## 八、设计原则

**这十条是本模块区别于普通数据看板的地方，评审与验收时应逐条对照。**

1. **只呈现已确认的数据。** 需要拍板的事在图谱平台完成。唯一例外是用户描述自身情况的输入。
2. **不生成没有依据的内容。** 学时、开课学期、课程名、设备型号一律不编。每条结论都要能说出取自哪个字段、哪份文件。
3. **不问用户答不上来的问题。** 不问"这门课教到什么程度"——教师自己说不准，问了只会得到噪音。只问"有没有"，程度从标准知识点推。
4. **让用户做减法而不是列举。** 先摆标准答案，用户只修正差异。
5. **如实标注精度边界。** 说明当前只能给到课程级，接入后可精确到知识点级。
6. **不做同类院校对比。** 无数据，不得暗示具备该能力。
7. **不把覆盖率当好坏判断。** 先描述结构，再让用户判断。
8. **不静默改变用户已看到的结论。** 设置变更时提示而非清空；报告内的图不跟随当前设置重绘。
9. **未填报 ≠ 没有。** 用户跳过的选填项不得被判定为"缺"，必须显示"尚未填报，无法比对"。
10. **同一份报告里的两个数不得口径打架。** 并列出现的指标必须各自写明口径。
11. **报告正文不调用模型。** 结论、排序、分档全部规则生成，同一批数据任意次运行输出一致；
    AI 只用于生成候选，经用户确认后才进入报告，见 F-49。

---

## 九、非功能要求

| 项 | 要求 |
|---|---|
| 形态 | 单页应用，嵌入既有管理端左侧菜单 |
| 响应式 | ≥1000px 为主布局；窄屏降级（侧栏折叠、多列变单列、图表横向滚动） |
| 主题 | 支持亮 / 暗色，跟随系统或用户设置 |
| 可访问性 | 键盘可操作（勾选项支持空格 / 回车）、ARIA 状态（含三态复选的 `mixed`）、焦点可见 |
| 打印 | 概况总览可单独打印；完整报告可打印；打印时隐藏导航与交互控件 |
| 错误处理 | 加载失败给整页错误态 + 重试，不显示半截数据 |
| 运行时 | 全流程无 JS 运行时错误 |
| 性能 | 按行业按需加载；首屏不阻塞于全量映射边 |

---

## 十、验收清单

### 10.1 流程

- [ ] 只做第 1 层就停 → 有报告，且只有概况总览与岗位面向
- [ ] 做到第 3 层 → 三个数齐全，雷达图、诊断结论、调整建议明细全部出现
- [ ] 第 4 层未做 → 第 4 档显示「本校资源尚未填报，无法比对」，**不显示为"缺"**
- [ ] 第 4 层做完且未勾某项 → 该项才显示为"缺"
- [ ] 中途改第 2 层岗位选择 → 出现变更提示条，**报告各节不被收起**，且实训室勾选不丢失
- [ ] 回到第 1 层重新点「确认专业」而未改变所选专业 → 岗位与课程勾选**保持不变**
- [ ] 多选专业 → 报告按专业分节

### 10.2 内容正确性

- [ ] 等级缺口能力项：观测点差集只显示目标等级独有的，不重复显示现状等级已有的
- [ ] 内容缺口能力项：分级递进呈现，标出目标等级与哪几级需实训承接
- [ ] 前置也在缺口的能力项：正确提示「需先补齐 X」
- [ ] 考核方式未命中规则：显示兜底文案，不硬凑教学形式建议
- [ ] **达成等级取映射边的最高等级**：同一能力被 L1 与 L2 两条边指向时判为 L2，不得取首条命中边
- [ ] **核心能力缺口数把"完全未覆盖"计入**：未覆盖的能力在 `att` 中无键，不得因 `undefined < n` 恒为假而漏计
- [ ] 零课程场景下，一页纸结论与诊断结论**不得互相矛盾**（不得一边说"无突出结构短板"一边说"N 条完全未覆盖"）
- [ ] 第 3 档无现成课程时显示「校本开发」，不留白
- [ ] 五档连续编号，未触发的显示「本次未触发」
- [ ] 第 4 档的输入集合含第 3 档推荐的新开课，且不含无缺口课程的实训要求
- [ ] 教学形式提示已标注"非图谱原生数据"并可展开看规则

### 10.3 边界与口径

- [ ] 未做测评时打开能力项详情：不出现「本校支撑情况」
- [ ] 产业趋势区标明"无来源证据"
- [ ] 未纳入本期的子行业不显示为"待拍板"
- [ ] 报告中无学时 / 开课学期 / 课程名 / 设备型号的生成内容
- [ ] 全文无"教到什么程度"类填报措辞

### 10.4 输出

- [ ] 「导出这一页」输出概况总览一页，排版正确
- [ ] Markdown 导出内容与页面一致，含提升路径一节
- [ ] 导出与打印件顶部标明批次号、生成时间、图谱版本与专业教学标准文件名
- [ ] 打印视图隐藏导航与交互控件

---

## 十一、实现要点与易错点

原型开发过程中实际踩过的坑，实现时重点规避。

| # | 易错点 | 正确做法 |
|---|---|---|
| 1 | 把"未填报"当成"确认没有" | 选填层未完成时一律显示"无法比对"，见 F-45 |
| 2 | 并列指标口径不一致（覆盖 vs 达标） | 每个指标旁注明口径，见 F-51 |
| 3 | `undefined` 参与数值比较被静默判为假 | 取自对象的等级值一律 `\|\| 0` 兜底后再比较，见 F-49.1 |
| 4 | 处方档位跳号 | 五档固定显示、连续编号，见 F-59 |
| 5 | 提示文案指向无效位置（"请切到单岗位查看"但切了也看不到） | 提供真实可达的出口，见 F-56 |
| 6 | 图例与实际渲染不符（写"斜纹"实际只有描边） | 图例文案与渲染实现必须同源或同步核对 |
| 7 | 院校数据渗入产业侧视图 | 行业全景默认不含任何院校信息，仅叠加时显示 |
| 8 | 展示口径与图谱产出口径不一致 | 凡展示用的分类（核心能力、覆盖面分层）必须来自图谱字段，不在前端自定义 |
| 9 | 重构时丢失旧实现的边界处理 | 迁移前先梳理旧实现的边界分支清单，逐条对照 |
| 10 | 新旧实现并存导致改错一套 | 旧实现确认废弃后彻底删除，不靠隐藏 tab 保留 |
| 11 | 回退一层时报告整份收起 | 回退只展开该层，**不得降级报告的渲染门槛**，见 F-40 |
| 12 | 重新确认某层时无条件重置下游选择 | 仅在该层选择**确实变化**时才重置，且需提示；见 F-40 |
| 13 | 同一条内容被多张卡片复读 | 检查挂载粒度是否比展示粒度更粗，见 F-62.1 |

---

## 十二、待确认事项（跨平台）

以下需图谱平台侧定成规则并随图谱产出，否则本模块只能在前端临时定义，换个前端就不一致。

| # | 事项 | 背景 | 建议 |
|---|---|---|---|
| 1 | **核心能力口径「与」还是「或」** | 现行「被 2 个以上岗位要求 **且** 有国家级来源」。实测：「服务礼仪与职业形象呈现」被 7 个岗位要求、覆盖面最广，却因无国标背书掉出核心集；「研学课程方案设计」只被 2 个岗位要求却因有国家级文件入选 | 若核心能力用于"告诉学校优先补哪些课"，覆盖面比法规背书更有指导意义。建议改为「或」，或拆成**通用核心 / 法定核心**两类标签 |
| 2 | **能力覆盖面分层口径** | 「全行业通用 / 子行业共性 / 岗位专属」三档不在图谱方案内，且"共性"档门槛依赖子行业岗位数、会随建图范围漂移 | 在图谱平台定义并作为能力项派生字段产出 |
| 3 | **产业趋势可追溯性** | 趋势由建图模型直接输出，无来源证据，只能标注不能核对 | 建议抽取环节记录 `fromTrend`（哪条趋势促成了这条新兴能力），使趋势可追溯到产出 |
| 4 | **人才培养方案解析** | 本期仅入口 | 实现后可把输入成本从三分钟压到三十秒，是四层流程的关键提效点 |

---

## 附录 · 术语表

| 术语 | 含义 |
|---|---|
| 四层能力图谱 | 行业 → 子行业 → 岗位 → 能力项 |
| 知识体系 / 知识图谱 | 专业 → 课程 → 模块 → 任务 → 操作流程 → 知识点 / 技能规范 / 标准 / 案例 |
| 课程体系 | 平台预置、按专业教学标准建的课程与知识点集合 |
| 双图谱映射 | 能力项 ↔ 知识点 / 技能规范之间的关系边 |
| 三级行为化描述 | 能力项在 L1/L2/L3 各级的行为描述、观测点、考核方式 |
| 观测点 | 可直接观察到的学生行为表现，每级 2~4 条 |
| 观测点差集 | 目标等级与现状等级观测点之差，即"要补的教学目标" |
| 服务等级 | 一条映射边把该能力支撑到哪一级 |
| 映射关系类型 | 直接支撑 / 部分支撑 / 前置铺垫 / 达标判据 |
| 来源等级 | L1 国家 > L2 行业标准 > L3 招聘 > L4 其他 |
| 标准对接岗位 | 专业教学标准中该专业"面向的职业岗位" |
| Gate 1 | 行业-专业配对，即标准岗位名 → 能力图谱岗位 ID 的映射 |
| 诊断批次 | 一次测评的结果快照，含专业、范围、输入与指标 |
