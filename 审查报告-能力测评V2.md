# 能力测评 V2（四层渐进式）审查报告

审查对象：`panorama.html` / `panorama_template.html`（4752 行，2026-09-14 06:33 版本）
方法：Playwright 无头浏览器实跑四层流程 + 边界场景 + 源码走查。
运行时错误：**`pageerror` 为空，控制台无 JS 报错**——下面全部是逻辑问题，不是崩溃。

行号均指 `panorama_template.html`。

---

## 一、必须先修（结论会说反话）

### P0-1　`coreGap` 踩了 JS 真值陷阱，导致「完全没开的核心能力」在结论里彻底消失

**位置**：3822 行，`v2Numbers()`

```js
const coreGap = Object.keys(reqMap).filter(id => abl[id].core && att[id] < reqMap[id]).length;
```

`att` 是 `v2Attained()` 返回的对象，**完全没有课程覆盖的能力项压根不在这个对象里**，取出来是 `undefined`。
而 `undefined < 3` 在 JS 里是 `false`——不是报错，是静默为假。

结果：**coreGap 只统计「教了但没教够」的，一条「完全没开」的核心能力都不计入。**
而这个阈值存在的唯一目的，恰恰是抓「整块内容没有承接」。

实测（勾全部 3 个专业、9 个岗位、第 3 层全部取消课程）：

| | 现行 | 正确 |
|---|---|---|
| coreGap | **0** | **12** |

同一页报告因此自相矛盾：

- 一页纸结论（最显眼的那行）：「能力覆盖与培养深度相对均衡，**无突出结构短板**」
- 下面的诊断结论：「**28 条能力项本校完全未覆盖**」

只选导游服务专业时更糟——结论落到「专业面向偏窄，属方向选择问题而非教学质量问题」，
第 5 档跟着给出 9 个岗位的「新增专业方向（要报备，最贵）」。
**学校真正的问题是课程还没勾，系统却建议去报新专业方向。**

这个 bug 有个隐蔽的地方：图谱默认把标准课程全勾上，所以正常路径下 coreGap 看起来是对的
（实测 10 = 10）。**只有当学校情况真的差时它才失效**，而那正是结论最要紧的时候。

**修法**：`(att[id] || 0) < reqMap[id]`。全文搜一遍 `att[` 的裸比较，同类还有没有。

---

### P0-2　`v2Conclusion` 整个函数是死代码，线上跑的是另一套逻辑

**位置**：3833 行定义，**全文零调用**（`v2StructureSentence` 只被它调用，一并死掉）。

报告里实际显示的是 4351 行的 `v2OverviewVerdict()`。两个函数前三个分支一模一样，
**但兜底分支完全不同**：

| | 兜底文案 |
|---|---|
| `v2Conclusion`（死的） | 调 `v2StructureSentence()`——按领域覆盖率说「集中在 X、Y 方向基本空白」 |
| `v2OverviewVerdict`（活的） | 「能力覆盖与培养深度相对均衡，无突出结构短板」 |

P0-1 的那句错话就出在这个兜底分支。而死掉的那套如果跑起来，会输出
「…「讲解与表达、资源与产品、行程组织与调度…」方向基本空白」——**是对的**。

也就是说：正确的兜底逻辑写好了，但没接上线。
两套并存迟早会改一套忘一套，建议删掉一套，只留一个真值来源。

---

### P0-3　点回上一层，报告整份塌掉；再点一次「确认专业」，前两层的填报被静默清空

**位置**：4717 行（回改）、4610 行 `v2InitFromMajors`

**现象一（报告塌陷）**——实测第 4 层完成后点第 2 层标题：

```
报告 HTML  28871 → 1843 字符
雷达 / 能力清单 / 标准符合度 / 诊断结论 / 处置建议  全部消失
而 V2.positions / courseIntent / leaves / labs  一个没丢
```

代码是 `V2.layer = n - 1`，而报告各节按 `V2.layer >= 3` 渲染。
数据都在、结论不给看，得重新点三次「确认」才回得来。
这和「每一层结束都有一份完整可用的报告，随时可以停」的设计前提直接冲突。

**现象二（静默清空，更严重）**——实测：

```
自定义完成：岗位 2 个 · 课程 3 门 · 知识节点 61 个 · 实训室 1 个
点回第 1 层（什么都没改，只是想看一眼选了哪个专业）
再点「确认专业，继续」
→ 岗位 3 个 · 课程 4 门 · 知识节点 79 个 · 实训室 1 个
```

`v2InitFromMajors()` 无条件把岗位和课程重置成标准答案。
函数里的注释写着「已确认过的更细选择（实训室）保留」——只保了实训室，
**岗位和课程勾选被覆盖掉了，没有任何提示。**

**修法建议**：
- 回改层时保留报告（把渲染门槛从 `V2.layer` 改成一个独立的 `maxLayer`，或者回改只把该层展开、不降级）
- `v2InitFromMajors` 只在专业集合**确实变化**时才重置；未变化则原样返回。若确实变了，给一句确认提示再重置。

---

## 二、数据口径问题（数字不假，但指向错了）

### P1-1　第 4 档「新建实训条件」查的是已开的课，而它的规则原文写的是要开的课

**位置**：3888 行 `v2LabGaps()`

规则文案：「**要开的课**有实训要求，而本校没有对应实训室。」
代码：`if(!V2.courseIntent.has(c.id)) return;` —— 只遍历**已保留的课**。

两个后果，方向相反：

**漏报**（该报的没报）——实测第 3 档建议新开《旅游数字营销与新媒体运营》来补「讲解类短视频策划与制作」，
这门课需要「新媒体内容制作实训室」+「在线店铺运营沙盘」，**学校两个都没有，第 4 档只字未提**。
这恰恰是第 4 档存在的意义：把「加一门课」背后真正的花钱项摆出来。现在预算被低估了。

**误报**（不该报的报了）——第 4 档实测列出的 4 项里，「语言实训室 / 旅游英语与跨文化服务」
所对应的课**只承接 1 条能力，且该能力没有缺口**。为一个没有缺口的能力提示「高成本新建实训条件」，
会稀释真正要花钱的那几条。

**修法**：第 4 档的输入应该是「第 3 档推荐的新课 ∪ 承接了缺口能力的已开课」的 `practiceReq`，
而不是全部已开课。

---

### P1-2　雷达图「请切到单岗位查看」指了一条死路

**位置**：4329 行

「全部」视图最多画 12 根轴，超出时提示：「另有 N 条未画，**请切到单岗位查看**」。
但单岗位视图也有 8 条上限（4246 行 `POS_RADAR_MAX`）。

实测（导游服务专业、3 个岗位、18 条能力，全部图裁掉 6 条）：

```
被裁掉的 6 条：服务礼仪与职业形象呈现、分众化讲解适配、导游讲解词编创、
              现场急救基本操作、跨文化沟通与外语服务、讲解类短视频策划与制作
其中在任何一张单岗位雷达里都看不到的：5 条
```

**6 条里 5 条按提示切过去也找不到。** 提示语在撒谎——这类小事对报告可信度的伤害不成比例地大。

**修法**（三选一）：改成「另有 N 条未画，见下方能力要求清单」（清单是全量的，这个指路是真的）；
或全部图不设上限、轴多时自动缩小字号；或提示里直接点名是哪几条。

---

### P1-3　「核心优势」按达标条数排，不按达标率

**位置**：4371 行

```js
const adv = Object.entries(byDom).filter(([,o]) => o.ok > 0)...slice(0,2);
```

只要某领域有 **1 条**能力达标，就进「核心优势」。实测领域分布：

| 领域 | 要求 | 达标 |
|---|---|---|
| 讲解与表达 | 5 | 2 |
| 服务与沟通 | 3 | 1 |
| 安全与应急 | 3 | 0 |
| 资源与产品 | 2 | 0（且完全没覆盖） |
| 数字工具与新媒体 | 1 | 0（且完全没覆盖） |

输出：「核心优势集中在「讲解与表达、**服务与沟通**」方向」——3 条里达标 1 条被称作核心优势。
同时**两个完全空白的领域一个字都没提**。

同一份报告里，死掉的 `v2StructureSentence` 用的是覆盖率（≥80% 算强、<50% 算空白），
是对的口径。两处「强/弱」定义不一致。

**修法**：改成达标率阈值（如 ≥60% 且至少 2 条），并把完全空白的领域一并说出来。

---

### P1-4　「依据文件」不去重

**位置**：4241 行 `v2Sources()`

```js
const t = (a.evidences||[]).filter(...).map(e => e.sourceTitle);
return t.join('、');
```

实测输出：`中华人民共和国旅游法、中华人民共和国旅游法`

V1 在同样的位置是有 `seen` Set 的（2842 行附近），V2 抄过来时漏掉了。
一行 `[...new Set(t)]` 的事，但出现在「依据文件」这种最需要显得严谨的字段上。

---

### P1-5　第 3 档没有推荐课时，什么提示都不给（`via` 是个永远走不到的分支）

**位置**：4479 行

```js
: (r.via === '校本开发（课程体系无现成课程）' ? `…课程体系无现成课程 · 校本开发` : '')
```

**`via` 这个字段全文从未被赋值过**（`grep via` 只有这一处）。
所以 `v2Prescriptions()` 里 `T[3]` 拿到 `course: null` 的项，渲染出来就是一条光秃秃的能力名，
没有任何「接下来怎么办」。

实测命中：**地方历史文化与非遗知识**——课程体系里确实没有现成课，
用户看到的却是一条没有下文的缺口。这条本该显示「校本开发」。

**修法**：在 `v2Prescriptions()` 里给 `course` 为空的项补上 `via`，或直接在渲染处判 `!r.course`。

---

### P1-6　第 2 层就给出「已达标 / 未覆盖」判定，同一页的三个数却说「完成第 3 层后出现」

**位置**：4557 行 `renderV2Report()`

第 2 层实测，能力要求清单已经打出：**已达标 3 条 · 未覆盖 1 条 · 等级缺口 14 条**。
而它正上方的「内容完整」「培养深度」两个数显示 `—`，副文案写着「完成第 3 层后出现」。

这两处用的是**同一个 `v2Attained()`**——数据来源完全一样，
区别只在于清单没做层级门控。而此刻的 `V2.leaves` 是 `v2InitFromMajors()` 按
「标准课程全开」预填的**假设值**，用户一个字都还没确认。

不是数字错，是**披露口径自相矛盾**：要么两处都显示（并明确标注「基于标准课程表假设，待第 3 层确认」），
要么两处都等到第 3 层。按你定的原则（「这里都是确认的数据」），建议前者——标注清楚假设来源。

---

## 三、交互体验问题

### P2-1　首次填报就弹「设置已变更」

**位置**：4635 行 `v2MarkStale()` —— `if(V2.layer >= 2) V2.stale = true;`

实测：处在第 2 层、正在**第一次**勾第 3 层的课程，点掉一门课就弹出
「设置已变更，报告已按当前设置重新计算，不会静默清空。」
第 3 层勾第 4 层实训室同理。

用户并没有「变更」任何东西，他在按流程正常填报。
这个提示应该只在**编辑已确认层**（层号 ≤ `V2.layer`）时出现，当前活动层的首次输入不算变更。

### P2-2　重走一遍流程，历史报告里多一条一模一样的记录

**位置**：4629 行 `v2Record()`，由 `v2Bump(4)` 触发。

实测：走完四层 → 历史 1 条；点回第 1 层再一路确认回来 → 历史 **2 条**，
两条的专业、岗位数、课程数、覆盖率完全相同。

`v2Record` 只在「首次到达第 4 层」时记，所以**停在第 3 层的用户一条历史都没有**——
而设计说的是「每一层结束都有一份完整可用的报告，随时可以停」。
建议：按 `stateKey`（V1 已有这个思路）去重，内容没变不新增；停在任意层也可存档。

### P2-3　档位编号跳号

实测渲染出的档位徽标是 **① ③ ④**，没有 ②（第 2 档无条目时被 `filter` 掉了）。
用户会以为漏了一档。建议空档也显示，写「无此类缺口」；或改成顺序编号、档名自带成本标签。

### P2-4　「支撑度 100%」和「培养深度 3/18」并存，没有一处说明二者口径不同

「专业教学标准符合度」里岗位徽标写「支撑度 X%」，口径是**有没有课覆盖**（>0 即算）；
概况里的「培养深度」口径是**有没有教到岗位要求的高度**。
同一份报告里一个岗位可以「支撑度 100%」而「达标 3/18」。

代码里有注释说明这个区分是刻意的，但**界面上一个字都没写**。
建议在徽标旁挂一个 ⓘ：「支撑度 = 该岗位要求的能力有课程覆盖的比例，不代表教到了岗位要求的高度」。

### P2-5　「仅保留标准对接岗位」名不副实

**位置**：`data-v2posall="0"` 分支——它是把 `V2.positions` **重置**为标准集合，
用户手动加进来的岗位会被去掉（符合「仅保留」），但**用户手动删掉的标准岗位会被加回来**（不符合）。
建议改名「恢复标准对接岗位」。

---

## 四、与 PRD 的差距（不是 bug，是没做）

- **F-49 每一节的数据来源声明**：报告里目前只有处置建议卡上有零星的「来自课程映射边」等小字，
  没有统一的 ⓘ 四栏（数据来源 / 你提供的信息 / 计算口径 / 精度边界），也没有报告末尾的「数据来源与口径」总表。
- **F-72 导出**：`v2Export()` 就是 `window.print()`，没有 Markdown 导出，
  也就没有 F-49 要求的「来源说明必须随导出走」。打印视图目前只有「导出这一页」用的 `print-dx` 有专门样式。
- **「导出这一页」按钮第 1 层就出现**：那时整页只有行业信息和三个空数（两个显示 `—`），导出没有意义。

---

## 五、修复优先级建议

| 顺序 | 项 | 理由 |
|---|---|---|
| 1 | P0-1 coreGap | 一行字的修改，但它让结论说反话 |
| 2 | P0-2 删掉死的那套结论逻辑 | 和 P0-1 同一处，一起改 |
| 3 | P0-3 回改不清空 | 用户填了半天的东西会没 |
| 4 | P1-1 第 4 档口径 | 直接关系到预算决策的准确性 |
| 5 | P1-4 / P1-5 | 各一两行，改完就好 |
| 6 | P1-2 雷达指路 | 可信度问题 |
| 7 | P2-1 / P2-2 | 体验噪音 |
| 8 | P1-3 / P1-6 / P2-3~5 | 口径与措辞，可一并处理 |
